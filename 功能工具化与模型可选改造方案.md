# 功能工具化与模型可选改造方案

## 1. 背景与目标

当前系统中，同一功能（如可灵图生视频）往往按模型拆成多个工具，导致：

- 用户端出现大量重复工具卡片，选择成本高。
- 模型增减需要新增/下线工具，维护复杂。
- 工具与模型强绑定，绑定错误时整块功能不可用。

本方案目标：

- 用户端按“功能”展示（一个功能一张卡片）。
- 用户进入功能后可自行选择支持该功能的模型。
- 保持现有任务、执行、计费链路可复用，平滑迁移。

---

## 2. 核心设计

### 2.1 概念拆分

- **功能工具（Tool）**：产品层能力入口，例如 `kling-image-to-video`、`kling-text-to-video`。
- **模型配置（ModelConfig）**：执行层模型定义，例如 `kling-v3`、`kling-v2-master`。
- **功能-模型绑定（ToolModelBinding）**：声明某个功能工具支持哪些模型、默认模型是谁。

### 2.2 运行时规则

1. 用户选择功能工具。
2. 前端加载该工具支持模型列表并展示选择器。
3. 用户选择模型发起任务，任务请求中携带 `modelConfigId`。
4. Worker 按任务内模型执行，不再依赖工具固定模型。

---

## 3. 数据库设计

## 3.1 新增表：`tool_model_bindings`

建议字段：

- `id` BIGINT PK
- `tool_id` BIGINT NOT NULL
- `model_config_id` BIGINT NOT NULL
- `is_default` TINYINT NOT NULL DEFAULT 0
- `enabled` TINYINT NOT NULL DEFAULT 1
- `sort_order` INT NOT NULL DEFAULT 0
- `created_at` DATETIME
- `updated_at` DATETIME

建议约束：

- `UNIQUE KEY uk_tool_model (tool_id, model_config_id)`
- `INDEX idx_tool_enabled (tool_id, enabled)`
- 业务约束：同一 `tool_id` 仅允许一个 `is_default=1`

## 3.2 兼容策略

- 保留 `ai_tools.model_config_id`（兼容旧逻辑）。
- 新逻辑优先读 `tool_model_bindings`，不存在时回退 `ai_tools.model_config_id`。

---

## 4. 后端改造

## 4.1 管理端（Admin）接口

新增接口：

- `GET /admin/tools/{toolId}/models`：读取工具支持模型列表。
- `PUT /admin/tools/{toolId}/models`：批量保存工具支持模型。
- `PATCH /admin/tools/{toolId}/models/default`：设置默认模型。

建议请求结构（示例）：

```json
{
  "bindings": [
    { "modelConfigId": 125, "enabled": true, "isDefault": true, "sortOrder": 10 },
    { "modelConfigId": 133, "enabled": true, "isDefault": false, "sortOrder": 20 }
  ]
}
```

## 4.2 用户端（User API）接口

在工具详情接口增加：

- `supportedModels`: 模型列表（`id/configCode/displayName/provider/isDefault/enabled`）
- `defaultModelConfigId`

## 4.3 任务创建与执行上下文

任务创建请求新增字段：

- `modelConfigId`（可选；若不传则使用工具默认模型）

执行模型解析顺序：

1. 任务传入的 `modelConfigId`
2. `tool_model_bindings` 中 `is_default=1` 且启用的模型
3. `ai_tools.model_config_id`（兼容兜底）

新增校验：

- 模型必须属于该工具支持列表。
- 模型 capability 必须满足工具执行类型（如 `VIDEO_GENERATION`）。

---

## 5. 前端改造

## 5.1 用户端

- 工具列表页：按“功能工具”展示，去重模型工具卡片。
- 工具详情页：新增模型选择组件（下拉/分组标签）。
- 提交任务时带上 `modelConfigId`。
- 任务详情展示：`功能名 + 实际模型名`。

## 5.2 管理端

工具编辑页新增“支持模型”模块：

- 仅展示 capability 匹配的模型。
- 支持启停、排序、默认设置。
- 支持快速复制绑定配置（便于批量配置）。

---

## 6. 迁移方案

## Phase 1：兼容上线

- 上线新表和新接口。
- 保留旧字段，业务不迁移也可运行。

## Phase 2：数据迁移

- 将同功能多工具归并为单工具（保留一个主工具）。
- 把原多条模型关系写入 `tool_model_bindings`。
- 工具卡片只保留功能工具。

## Phase 3：清理收口

- 下线重复工具。
- 清理历史仅模型维度的工具命名和描述。
- 逐步弱化 `ai_tools.model_config_id` 的使用。

---

## 7. 错误码与可观测性

建议新增错误码：

- `MODEL_NOT_ALLOWED_FOR_TOOL`
- `MODEL_NOT_ENABLED`
- `MODEL_CAPABILITY_MISMATCH`

日志与任务错误建议包含：

- `toolCode`
- `modelConfigId`
- `modelName`
- 上游 `request_id`、`error_code`、`message`

---

## 8. 验收标准（DoD）

- 同一功能在用户端只出现一张卡片。
- 功能详情内可切换多个模型并成功创建任务。
- 不同模型执行结果中能准确记录模型名。
- 非法模型选择能被后端明确拦截并返回可读错误。
- 旧任务和旧工具在兼容期内正常运行。

---

## 9. 实施建议（本项目优先级）

建议先落地视频功能（可灵）作为第一批试点：

1. 先做 `kling-text-to-video`、`kling-image-to-video` 两个功能工具。
2. 绑定多个可用模型（例如 `kling-v3`、`kling-v2-master`、`kling-v1`）。
3. 验证用户端模型切换、任务追踪、计费链路。
4. 通过后再推广到图像生成、数字人等其它工具域。

