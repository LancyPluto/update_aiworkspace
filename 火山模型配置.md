# 火山引擎 / 豆包模型配置手册

供后台「AI 模型配置」与工具表单使用。火山视频/图像已收敛为 **2 个网关 modelConfig + 2 个工具**；同模态内通过表单 `model` 字段选择版本（Suno 式）。文本 Seed 2.0 pro/lite **暂保留独立 modelConfig**（Phase 1b 待计费验证后合并）。

> 价格核实日：2026-05-25（见 [模型配置.md](模型配置.md)）。  
> 表单规范：[docs/同系列模型版本表单配置规范.md](docs/同系列模型版本表单配置规范.md)  
> 配置包 patch：`scripts/patch_volcengine_config_bundle.py`

## 一、网关 modelConfig

| configCode | 显示名 | provider | 默认 modelName | executionTask | 计费 | 文档 |
| --- | --- | --- | --- | --- | --- | --- |
| `volcengine-gateway-video` | 火山 · 视频生成 | `seedance` | `doubao-seedance-1-5-pro-251215` | `video_generation` | PER_CALL ¥0.875/次（5s 720p） | [创建视频生成任务](https://www.volcengine.com/docs/82379/1520757) |
| `volcengine-gateway-image` | 火山 · 图像生成 | `volcengine_images` | `doubao-seedream-4-5-251128` | `image_generation` | PER_CALL ¥0.25/张 | [图片生成 API](https://www.volcengine.com/docs/82379/1541523) |

文本（Phase 1b，暂未合并）：

| configCode | provider | modelName | 文档 |
| --- | --- | --- | --- |
| `volcengine-doubao-seed-2-pro` | `openai_compatible` | `doubao-seed-2-0-pro-260215` | [Chat API](https://www.volcengine.com/docs/82379/1494384) |
| `volcengine-doubao-seed-2-lite` | `openai_compatible` | `doubao-seed-2-0-lite-260215` | 同上 |

### 1.1 公共字段

| 字段 | 建议值 |
| --- | --- |
| `channelCode` / `channelLabel` / `channelIconAsset` | `volcengine` / `火山引擎 / 豆包` / `doubao` |
| `vendorAccountRef` | `volcengine::默认账户` |
| `baseUrl` | `https://ark.cn-beijing.volces.com`（后端规范化为 `/api/v3`） |
| `consoleUrl` | https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey |
| `balanceUrl` | https://console.volcengine.com/finance/overview |

### 1.2 extraAuthJson

视频网关：

```json
{
  "createPath": "/contents/generations/tasks",
  "resultPath": "/contents/generations/tasks/{task_id}"
}
```

图像网关（账户发现默认模板）：

```json
{
  "imageInputMode": "jsonImageArray",
  "endpointPath": "/images/generations",
  "responseFormat": "url",
  "readTimeoutSeconds": 600,
  "connectionRetries": 2,
  "sslEofRetries": 2
}
```

## 二、API 对照与表单 model 枚举

### 2.1 视频（`volcengine-video`）

| model（Endpoint ID） | 说明 |
| --- | --- |
| `doubao-seedance-1-5-pro-251215` | 推荐；支持有声（Worker 待接 `generate_audio`） |
| `doubao-seedance-1-0-pro-250528` | 1.0 Pro |
| `doubao-seedance-1-0-pro-fast-251015` | 1.0 Pro Fast |

| API 参数 | 表单 fieldKey | Worker |
| --- | --- | --- |
| `model` | `model` | `resolve_volcengine_task_model` |
| `content[].text` | `prompt` | 已实现 |
| `content[].image_url` | `imageUrl` | 已实现（可选） |
| `duration` | `duration` | 已实现 |
| `ratio` | `aspectRatio` | 已实现 |
| `resolution` | `resolution` | 已实现 |
| `generate_audio` | — | **待接入** |

### 2.2 图像（`volcengine-image`）

| model | 说明 |
| --- | --- |
| `doubao-seedream-4-5-251128` | 推荐 |
| `doubao-seedream-5-0-260128` | Seedream 5.0 |

| API 参数 | 表单 fieldKey | Worker |
| --- | --- | --- |
| `model` | `model` | `resolve_volcengine_task_model` |
| `prompt` | `prompt` | 已实现 |
| `size` / `image_size` | `aspectRatio` | 已实现 |
| `n` | `count` | 已实现 → `batch_size` |
| 参考图 | `imageUrl` | 已实现 |

## 三、工具与绑定

| toolCode | 工具名 | 绑定网关 | 状态 |
| --- | --- | --- | --- |
| `volcengine-video` | 豆包视频生成 | `volcengine-gateway-video` | ONLINE |
| `volcengine-image` | 豆包图像生成 | `volcengine-gateway-image` | ONLINE |

管理端选项预设：`volcengine_model_video`、`volcengine_model_image`（见 `admin-frontend/lib/tool-fields.ts`）。

## 四、迁移对照

| 旧 configCode | 新入口 |
| --- | --- |
| `volcengine-seedance` | `volcengine-gateway-video` |
| `seedance_video_generation` | `volcengine-gateway-video` |
| `volcengine-seedream` | `volcengine-gateway-image` |

## 五、定价规则建议

视频网关基准 `unitPrice=0.875`（5s×0.175元/秒），可按 `resolution` / `duration` 倍率扩展。

图像网关基准 `unitPrice=0.25`，`pricingRules` 按 `count` VALUE 倍率。
