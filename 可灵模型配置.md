# 可灵模型配置手册

供后台「AI 模型配置」与工具表单使用。可灵已收敛为 **7 个网关 modelConfig + 7 个工具**；同 API 任务类型内通过表单 `model` 字段选择版本（Suno 式），不再为每个版本单独建 modelConfig 行。

> 价格核实日：2026-06-15（以可灵官方控制台与账单为准）。  
> 导入：使用仓库根目录 `ai-tool-market-config-2026-06-15.json`，或在管理端导出配置包。含明文密钥时勿提交 Git。详见 [docs/配置包导入导出-维护指南.md](docs/配置包导入导出-维护指南.md)。  
> 表单规范：[docs/同系列模型版本表单配置规范.md](docs/同系列模型版本表单配置规范.md)  
> 参考列表字段（imageList / videoList / elementList）：[docs/可灵参考列表表单配置说明.md](docs/可灵参考列表表单配置说明.md)

## 一、网关 modelConfig（系统配置 7 条）

| configCode | 显示名 | 默认 modelName | API 任务 | 计费 | 文档 |
| --- | --- | --- | --- | --- | --- |
| `kling-gateway-text-to-video` | 可灵 · 文生视频 | `kling-v3` | text2video | PER_SECOND ¥0.6/秒 | [textToVideo](https://www.klingai.com/document-api/apiReference/model/textToVideo) |
| `kling-gateway-image-to-video` | 可灵 · 图生视频 | `kling-v3` | image2video | PER_SECOND ¥0.6/秒 | [imageToVideo](https://www.klingai.com/document-api/apiReference/model/imageToVideo) |
| `kling-gateway-motion-control` | 可灵 · 动作控制 | `kling-v3` | motion_control | PER_SECOND ¥0.6/秒 | [motionControl](https://www.klingai.com/document-api/apiReference/model/motionControl) |
| `kling-gateway-multi-image-to-video` | 可灵 · 多图参考生视频 | `kling-v1-6` | multi_image2video | PER_SECOND ¥0.6/秒 | [multiImageToVideo](https://www.klingai.com/document-api/apiReference/model/multiImageToVideo) |
| `kling-gateway-omni-video` | 可灵 · Omni 视频 | `kling-v3-omni` | omni_video | PER_SECOND ¥0.6/秒 | [OmniVideo](https://www.klingai.com/document-api/apiReference/model/OmniVideo) |
| `kling-gateway-image-generation` | 可灵 · 文生图 | `kling-v3` | image_generation | PER_CALL ¥0.019998/次 | [imageGeneration](https://www.klingai.com/document-api/apiReference/model/imageGeneration) |
| `kling-gateway-omni-image` | 可灵 · Omni 生图 | `kling-image-o1` | omni_image | PER_CALL ¥0.019998/次 | [OmniImage](https://www.klingai.com/document-api/apiReference/model/OmniImage) |

### 1.1 extraAuthJson 结构

```json
{
  "accessKey": "...",
  "secretKey": "...",
  "apiTask": "image2video",
  "createPath": "/v1/videos/image2video",
  "resultPath": "/v1/videos/image2video/{task_id}"
}
```

Worker 从 `apiTask` / `createPath` 路由 API；实际 `model_name` 以表单 `params.model` 为准。

### 1.2 视频参数定价规则（pricingRules）

基准 `PER_SECOND`，`duration` 由定价引擎自动乘算。额外倍率示例：

| paramKey | matchOp | matchValue | factor | 说明 |
| --- | --- | --- | --- | --- |
| `mode` | EQ | `pro` | 1.5 | pro 相对 std |
| `sound` | EQ | `on` | 1.2 | 开启声音 |
| `model` | EQ | `kling-v2-5-turbo` | 0.8 | Turbo 折价示例 |

生图 / Omni 生图网关可加 `count` + `VALUE` 倍率；Omni 生图可加 `resolution=2k` 倍率。SQL 种子：`sql/064_seed_kling_pricing.sql`。

## 二、API 对照表（model_name 与表单映射）

以下 `model_name` 枚举与 [SynStar 可灵平台 API 镜像](http://docs.kkiai.com/Other-Model-APIs/kling-platform-api/) 及官方 `docsUrl` 对齐，作为 patch 脚本与 `OPTION_PRESETS` 的权威来源。

### 2.1 text2video（`kling-text-to-video`）

| model_name | 表单 model value |
| --- | --- |
| kling-v3, kling-v2-5-turbo, kling-v2-1-master, kling-v2-master, kling-v1-6, kling-v1 | 同名 |

| API 参数 | 表单 fieldKey | 说明 |
| --- | --- | --- |
| prompt | prompt | 必填（multiShot=false 时） |
| multi_shot | multiShot | 必填，默认 false |
| shot_type | shotType | multiShot=true 时 customize |
| multi_prompt | multiPrompt | 多镜头分镜 |
| aspect_ratio | aspectRatio | 16:9 / 9:16 / 1:1 |
| duration | duration | 3–15 秒（V3） |
| mode | mode | std / pro |
| resolution | resolution | 720p / 1080p |
| sound | sound | on / off，仅 V2.6+ / V2.5 Turbo / V3（visibleWhen） |
| cfg_scale | cfgScale | 可选 |
| negative_prompt | negativePrompt | 可选 |

### 2.2 image2video（`kling-image-to-video`）

| model_name |
| --- |
| kling-v3, kling-v2-6, kling-v2-5-turbo, kling-v2-1-master, kling-v2-1, kling-v2-master, kling-v1-6, kling-v1-5, kling-v1 |

| API 参数 | 表单 fieldKey | 说明 |
| --- | --- | --- |
| image | imageUrl | 必填首帧 |
| image_tail | imageTail | 可选，建议 pro 模式 |
| prompt, duration, mode, aspect_ratio, resolution, sound, negative_prompt | 同名字段（camelCase） | sound 同 visibleWhen |

### 2.3 motion_control（`kling-motion-control`）

| model_name | kling-v3, kling-v2-6 |
| image_url | imageUrl |
| video_url | videoUrl |
| keep_original_sound | keepOriginalSound | yes / no |
| character_orientation | characterOrientation | image / video |
| mode | mode | std / pro |

### 2.4 multi_image2video（`kling-multi-image-to-video`）

| model_name | kling-v1-6（官方唯一枚举） |
| image_list | imageList（multi_image） |
| prompt, mode, duration, aspect_ratio, negative_prompt | 对应表单字段 |

### 2.5 omni_video（`kling-omni-video`）

| model_name | kling-v3-omni, kling-video-o1 |
| image_list / video_list / element_list | imageList / videoList / elementList |
| multi_shot, shot_type, multi_prompt | multiShot / shotType / multiPrompt |
| duration | 3–15 秒（v3-omni）；video-o1 常用 5/10 |

### 2.6 image_generation（`kling-image-generation`）

| model_name | kling-v3, kling-v2-1, kling-v2-new, kling-v2, kling-v1-5, kling-v1 |
| n | count | 1–4 |
| resolution | resolution | 1k / 2k |
| image | imageUrl | 可选参考图 |

### 2.7 omni_image（`kling-omni-image`）

| model_name | kling-image-o1, kling-v3-omni |
| image_list | imageList | 可选，multi_image |
| resolution | resolution | 1k / 2k |
| n | count | 1–9 |
| aspect_ratio | aspectRatio | 16:9, 9:16, 1:1, 4:3, 3:4, 3:2, 2:3, 21:9, auto |
| result_type | resultType | single / series |

路径：`POST /v1/images/omni-image`，`GET /v1/images/omni-image/{task_id}`。

## 三、工具与表单 model 版本

| toolCode | 工具名 | 绑定网关 | 默认上线 | 表单 model 可选版本 |
| --- | --- | --- | --- | --- |
| `kling-text-to-video` | 可灵文生视频 | `kling-gateway-text-to-video` | OFFLINE | v3, v2-5-turbo, v2-1-master, v2-master, v1-6, v1 |
| `kling-image-to-video` | 可灵图生视频 | `kling-gateway-image-to-video` | **ONLINE** | 9 个官方 image2video 版本 |
| `kling-motion-control` | 可灵动作控制 | `kling-gateway-motion-control` | OFFLINE | kling-v3, kling-v2-6 |
| `kling-multi-image-to-video` | 可灵多图参考生视频 | `kling-gateway-multi-image-to-video` | OFFLINE | kling-v1-6 |
| `kling-omni-video` | 可灵 Omni 视频 | `kling-gateway-omni-video` | **ONLINE** | kling-v3-omni, kling-video-o1 |
| `kling-image-generation` | 可灵生图 | `kling-gateway-image-generation` | **ONLINE** | v3, v2-1, v2-new, v2, v1-5, v1 |
| `kling-omni-image` | 可灵 Omni 生图 | `kling-gateway-omni-image` | **ONLINE** | kling-image-o1, kling-v3-omni |

管理端字段编辑器可使用选项预设：`kling_model_image2video`、`kling_model_text2video`、`kling_model_omni_image` 等（见 `admin-frontend/lib/tool-fields.ts`）。

### 3.1 参考列表字段（勿用手填 JSON）

| toolCode | 字段 | fieldType | 说明 |
| --- | --- | --- | --- |
| `kling-multi-image-to-video` | `imageList` | `multi_image` | 必填，1~7 张参考图 |
| `kling-omni-video` | `imageList` | `multi_image` | 可选参考图 |
| `kling-omni-video` | `videoList` | `multi_video` | 可选参考视频 |
| `kling-omni-video` | `elementList` | `subject_element_list` | 可选主体参考 |
| `kling-omni-image` | `imageList` | `multi_image` | 可选参考图 |

完整配置示例、Worker 映射与迁移清单见 [docs/可灵参考列表表单配置说明.md](docs/可灵参考列表表单配置说明.md)。

## 四、后台字段建议

| 字段 | 建议值 |
| --- | --- |
| `provider` | `kling_video` |
| `baseUrl` | `https://api-beijing.klingai.com` |
| `apiKey` | 留空（推荐走 `extraAuthJson` AK/SK） |
| `capabilities` | 视频网关 `VIDEO_GENERATION`；生图/Omni 生图 `IMAGE_GENERATION` |
| `billingUnit` | 视频 `PER_SECOND`；生图 `PER_CALL` |
| `timeoutSeconds` | 推荐 `600` |

## 五、下期 API（仅文档占位）

以下 API 本期未接入 Worker：

- [lipSync](https://www.klingai.com/document-api/apiReference/model/lipSync)
- [avatar](https://www.klingai.com/document-api/apiReference/model/avatar)
- [multiImageToImage](https://www.klingai.com/document-api/apiReference/model/multiImageToImage)
- [imageExpansion](https://www.klingai.com/document-api/apiReference/model/imageExpansion)

## 六、迁移对照（旧 → 新）

| 旧 toolCode / modelConfig | 新入口 |
| --- | --- |
| `kling-v3-image-to-video` 等全部版本型图生视频工具 | `kling-image-to-video` + 表单 `model` |
| `kling-v3-text-to-video` | `kling-text-to-video` |
| `kling-v3-motion-control`, `kling-v2-6-motion-control` | `kling-motion-control` |
| `kling-v3-multi-image-reference` | `kling-multi-image-to-video` |
| `kling-v3-omni`, `kling-video-o1-omni` | `kling-omni-video` |
| `kling-image-generation-v1/v2-1/v3` | `kling-image-generation` |
| （无） | `kling-omni-image`（Omni 生图） |

配置包 patch 脚本：`scripts/patch_kling_config_bundle.py`。
