# 大模型 / API 平台配置手册

供后台「AI 模型配置」使用。每个模型单独一行，含**控制台 / 余额 / 模型 API 文档 / 官方价格 / 影响成本参数 / 后台填法**。

> 链接均为官方域名。`cost_amount` 公式见文首；工具 `estimatedCreditCost` 需单独定算力。  
> **价格核实日：2026-05-25**（来自各厂商价目页，活动价、缓存命中、Batch 半价未单列，以账单为准）。  
> **文档链接**：`docsUrl` 填本表「模型 API 文档」列（对应该 `modelName` 的调用说明，非平台首页）。  
> **导入包**：仓库内全量示例为 `ai-tool-market-config-2026-06-12.json`（根目录）；也可在管理端「系统配置」或「工具管理」导出。含密钥时勿提交 Git。格式与导入流程见 [docs/配置包导入导出-维护指南.md](docs/配置包导入导出-维护指南.md)。  
> 历史专项包（如 `ai-tool-market-config-domestic-models-2026-05-25.json`）可能不在 git 中，需要时从管理端导出。

**成本公式**（`billing_usage_logs`）：

```text
cost_amount = promptTokens/1e6×输入单价/1M + completionTokens/1e6×输出单价/1M + billableUnits×unitPrice（仅 PER_CALL）
```

| 计费单位 | 后台填法概要 |
| --- | --- |
| `TOKEN_PER_M` | 填 `inputTokenPricePer1m`、`outputTokenPricePer1m`（元/百万 token），`unitPrice=0` |
| `PER_CALL` | 填 `unitPrice`（元/次、元/张、元/秒等）；**必须与「影响成本参数」默认档一致** |
| `IMAGE_TOKEN` | 填输入/输出 token 单价/1M；无 usage 时 Worker 按 `quality`+`size`+`n` 估 token |

**价格列说明**：国内（§二、§三）为 **元/百万 tokens**；国际（§一）为 **USD/百万 tokens**。

---

## 一、国际主流（文本）

| 平台 | modelName | 价格（USD/1M） | 影响成本参数 | 计费 | 控制台 | 余额 | 模型 API 文档 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| OpenAI | `gpt-5.5` | 入 **$5** / 出 **$30** | `model`；`messages`；`max_completion_tokens`；`reasoning_effort`（none/low/medium/high/xhigh）；`service_tier`；多模态；`tools` | TOKEN | [API Keys](https://platform.openai.com/api-keys) | [用量](https://platform.openai.com/usage) | [GPT-5.5 模型与 Responses API](https://developers.openai.com/api/docs/models/gpt-5.5) |
| Anthropic | `claude-sonnet-4-6` | 入 **$3** / 出 **$15** | `model`；`messages`；`max_tokens`；`thinking`（adaptive / enabled）；`tools`；Batch 半价 | TOKEN | [Keys](https://console.anthropic.com/settings/keys) | [Billing](https://console.anthropic.com/settings/billing) | [Claude Sonnet 4.6 扩展思考与 Messages API](https://platform.claude.com/docs/en/build-with-claude/extended-thinking) |
| Google | `gemini-2.5-flash` | 入 **$0.15** / 出 **$0.60** | `model`；`contents`；`maxOutputTokens`；图/音/视频输入 token；思考 token | TOKEN | [API Key](https://aistudio.google.com/apikey) | [结算](https://console.cloud.google.com/billing) | [Gemini 2.5 Flash](https://ai.google.dev/gemini-api/docs/models/gemini-2.5-flash) |
| Google | `gemini-2.5-pro` | 入 **$1.25** / 出 **$10**（≤200k） | 同上；**>200k 输入档加价** | TOKEN | 同上 | 同上 | [Gemini 2.5 Pro](https://ai.google.dev/gemini-api/docs/models/gemini-2.5-pro) |

---

## 二、国内主流（文本 / 通用 API）

| 平台 | modelName | 价格（元/1M） | 影响成本参数 | 计费 | configCode | 控制台 | 余额 | 模型 API 文档 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 阿里云百炼 | `qwen3.7-max` | 入 **2.5** / 出 **10**（0–32K 档） | `model`；输入分档（≤32K / 32–128K / 128–256K）；`messages`；`max_tokens`；`enable_thinking`；多模态附件 | TOKEN | `aliyun-qwen3-7-max` | [百炼 Key](https://bailian.console.aliyun.com/?tab=model#/api-key) | [阿里云费用](https://usercenter2.aliyun.com/home) | [Qwen3.7 Max 深度思考与 OpenAI 兼容](https://help.aliyun.com/zh/model-studio/deep-thinking) |
| 阿里云百炼 | `qwen3.5-plus` | 入 **0.8** / 出 **4.8** | `model`；输入分档；`enable_thinking`；`messages`；`max_tokens`；图/视频多模态 | TOKEN | `aliyun-qwen3-5-plus` | 同上 | 同上 | [Qwen3.5 Plus 视觉理解（图/视频）](https://help.aliyun.com/zh/model-studio/vision) |
| 阿里云百炼 | `qwen3.5-flash` | 入 **0.15** / 出 **1.5** | 同上（轻量档） | TOKEN | `aliyun-qwen3-5-flash` | 同上 | 同上 | [Qwen3.5 Flash 深度思考](https://help.aliyun.com/zh/model-studio/deep-thinking) |
| 智谱 GLM | `glm-5` | 入 **4** / 出 **18** | `model`；`messages`；`max_tokens`；**`thinking.type`**（enabled 增推理 token）；`tools` | TOKEN | `zhipu-glm-5` | [开放平台 Key](https://open.bigmodel.cn/usercenter/apikeys) | [计费](https://open.bigmodel.cn/usercenter/billing) | [GLM-5 模型与 Chat Completions](https://docs.bigmodel.cn/cn/guide/models/text/glm-5) |
| 月之暗面 | `kimi-k2.6` | 入 **6.84** / 出 **28.8** | `model`；`messages`；**`max_completion_tokens`**；`thinking`（enabled/disabled）；多模态 | TOKEN | `kimi-k2-6` | [控制台 Key](https://platform.moonshot.cn/console/api-keys) | [计费](https://platform.moonshot.cn/console/billing) | [Kimi K2.6 快速开始](https://platform.kimi.com/docs/guide/kimi-k2-6-quickstart) |
| 腾讯混元 | `hunyuan-turbos-latest` | 入 **0.8** / 出 **2** | `model`；`Messages`；`MaxTokens`；`enable_enhancement` | TOKEN | `tencent-hunyuan-turbos` | [混元](https://console.cloud.tencent.com/hunyuan/start) | [费用中心](https://console.cloud.tencent.com/expense/overview) | [hunyuan-turbos-latest OpenAI 兼容调用](https://cloud.tencent.com/document/product/1729/111007) |
| 火山方舟 | `doubao-seed-2-0-pro-260215`（别名 `doubao-seed-2.0-pro`） | 入 **3.2** / 出 **16**（0–32K） | `model`；`messages`；**`reasoning_effort`**；多模态 token；`max_tokens` | TOKEN | `volcengine-doubao-seed-2-pro` | [方舟 Key](https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey) | [费用中心](https://console.volcengine.com/finance/overview) | [对话 Chat API（Seed 2.0）](https://www.volcengine.com/docs/82379/1494384) |
| 火山方舟 | `doubao-seed-2-0-lite-260215`（别名 `doubao-seed-2.0-lite`） | 入 **0.6** / 出 **3.6**（0–32K） | 同上 | TOKEN | `volcengine-doubao-seed-2-lite` | 同上 | 同上 | [对话 Chat API（Seed 2.0）](https://www.volcengine.com/docs/82379/1494384) |
| DeepSeek | `deepseek-v4-flash` | 入 **1** / 出 **2**（缓存未命中） | `model`；`messages`；`max_tokens`；**缓存命中/未命中**；`thinking` | TOKEN | `deepseek-v4-flash` | [Keys](https://platform.deepseek.com/api_keys) | [用量](https://platform.deepseek.com/usage) | [DeepSeek V4 Flash 首次调用与模型列表](https://api-docs.deepseek.com/) |
| MiniMax | `MiniMax-M2.7` | 入 **2.1** / 出 **8.4** | `model`；`messages`；`max_tokens`；`temperature` | TOKEN | `minimax-m2-7` | [接口密钥](https://platform.minimaxi.com/user-center/basic-information/interface-key) | [充值](https://platform.minimaxi.com/user-center/payment) | [MiniMax-M2.7 OpenAI 兼容 Chat](https://platform.minimaxi.com/docs/api-reference/text-chat-openai) |

**默认推荐（新工具绑模型）**：对话 `qwen3.5-plus` 或 `deepseek-v4-flash`；Agent/编程 `qwen3.7-max` 或 `glm-5`；低成本 `qwen3.5-flash`。

---

## 三、多模态 / 视频 / 图像 / 语音

| 平台 | modelName | 价格（元） | 按次单价默认档 | 影响成本参数 | 计费 | configCode | 控制台 | 余额 | 模型 API 文档 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 快手可灵 | `kling-v3-omni` | 约 **0.6 元/秒**（std） | **3.0** 元/次（5s std 720p） | **`model_name`**；**`duration`**；**`mode`**；**`resolution`**；**`aspect_ratio`**；**`sound`**；文生/图生/首尾帧 | PER_CALL | `kling-v3-omni` | [可灵开放平台](https://app.klingai.com/cn/dev/api-key) | [资源包](https://app.klingai.com/cn/dev/resource-pack) | [Omni 视频 API](https://www.klingai.com/document-api/apiReference/model/OmniVideo) |
| 火山方舟 | `doubao-seedance-1-5-pro-251215` | 720p 无声 **0.175 元/秒** | **0.875** 元/次（×5s） | **`duration`**；**`ratio`**；**`resolution`**；有声；`image_url` | PER_CALL | `volcengine-gateway-video` | [方舟 Key](https://console.volcengine.com/ark/region:ark+cn-beijing/apiKey) | [费用中心](https://console.volcengine.com/finance/overview) | [创建视频生成任务 API](https://www.volcengine.com/docs/82379/1520757) |
| 火山方舟 | `doubao-seedream-4-5-251128` | 约 **0.25 元/张** | **0.25** 元/张 | **`size`** / `image_size`；**`n`**；参考图数量 | PER_CALL | `volcengine-gateway-image` | 同上 | 同上 | [图片生成 API（Seedream）](https://www.volcengine.com/docs/82379/1541523) |
| SiliconFlow | `Tongyi-MAI/Z-Image-Turbo` | **$0.005/张** | **0.036** 元/张 | **`image_size`**；**`batch_size`** / 张数；`seed` | PER_CALL | `siliconflow-z-image-turbo` | [SiliconFlow Key](https://cloud.siliconflow.cn/account/ak) | [余额](https://cloud.siliconflow.cn/account/balance) | [Images Generations（Z-Image-Turbo）](https://docs.siliconflow.cn/cn/api-reference/images/images-generations) |
| SiliconFlow | `FunAudioLLM/CosyVoice2-0.5B` | **7.15 元/百万 UTF-8 字节** | 按百万字节 1 单位 | 文本字节数；**`voice`**；`speed`；`format` | PER_CALL | `siliconflow-cosyvoice` | 同上 | 同上 | [Create Speech（CosyVoice2）](https://docs.siliconflow.cn/cn/api-reference/audio/create-speech) |
| MiniMax | `speech-2.8-hd` | **3.5 元/万字符** | 万字符 = 1 单位 | 文本长度；**`voice_id`**；HD 档；`speed`；`format` | PER_CALL | `minimax-speech-hd` | [接口密钥](https://platform.minimaxi.com/user-center/basic-information/interface-key) | [充值](https://platform.minimaxi.com/user-center/payment) | [同步语音合成 HTTP（speech-2.8-hd）](https://platform.minimaxi.com/docs/api-reference/speech-t2a-http) |

**工具字段映射（本项目 Worker 已接）**：

| 供应商 | 工具/任务字段 → API |
| --- | --- |
| 可灵视频 | `duration`→`duration`；`mode`→`mode`；`aspectRatio`→画幅；`resolution`→清晰度；`sound`→`sound`；`imageUrl`/`imageTail`→`image`/`image_tail` |
| Seedance | `duration`→秒；`aspectRatio`→`ratio`；参考图→`image_url` |
| 生图（SF/方舟） | `aspectRatio`/`imageSize`→`image_size`；`count`→`batch_size` |
| TTS | 文本→`input`；音色→`voice` / `voice_id` |

---

## 四、通用说明

### 4.1 三链接怎么填

1. **控制台**：API Keys / 密钥管理。  
2. **余额**：充值、账单、用量（与控制台同账号体系）。  
3. **模型 API 文档**：必须能直接看到该 `modelName` 的请求示例、参数与限制；填到后台 `docsUrl`。

### 4.2 多档价格怎么建配置

| 场景 | 做法 |
| --- | --- |
| 可灵 5s std 720p vs 10s pro 1080p | 各建一条 `configCode`，`unitPrice` 按该档填写 |
| Qwen3-Max 输入 ≤32K vs 超长 | 默认按 ≤32K 档填单价；超长单独工具或加价说明 |
| DeepSeek 缓存命中 | 记账按实际命中；预估算力可按未命中保守估 |

### 4.3 `unitPrice` 与 `estimatedCreditCost`

1. **TOKEN 模型**：单价填标准档；算力预估用默认 `max_tokens` + 典型输入长度；思考模式输出 token 建议 ×≥1.5。  
2. **PER_CALL 视频**：`unitPrice` = 单价/秒 × 默认 `duration`（与默认 mode+resolution 一致）。  
3. **PER_CALL 生图**：`unitPrice` = 默认 `n` 张 × 单张价。  
4. **TTS**：`unitPrice` 对应默认字数/字节/万字符单位。

### 4.4 上新检查清单

**密钥 → 价目 + 默认档参数 → 试跑 → 对账 `cost_amount` → Enabled**；`docsUrl` 用本文「模型 API 文档」列，勿填厂商文档首页。

### 4.5 相关文档

- [配置包导入导出 — 维护指南](docs/配置包导入导出-维护指南.md)  
- [统一 API 管理 — 开发文档](docs/统一API管理-开发文档.md)  
- [可灵模型配置.md](可灵模型配置.md)  
- [火山模型配置.md](火山模型配置.md)  
- `backend/src/main/resources/model-providers.yml`  
- 全量导入包：`ai-tool-market-config-2026-06-12.json`（或管理端导出）
