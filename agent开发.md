# Agent 开发进度评估

## 当前完成度

如果按“Agent 基础架构是否已经搭起来”来看，当前完成度大约在 **60% 到 70%**。

如果按“是否已经达到一个真正可用、能稳定替用户完成任务的生产级 Agent”来看，当前完成度更接近 **40% 到 50%**。

原因是：**框架、链路、界面、事件系统、模型接入、确认流、预算控制这些骨架已经基本齐了；但 Agent 最核心的‘真的调用工具并拿到真实结果’这一段还没有完全做实，目前更像是一个可以演示主流程的 Alpha 版本。**

## 已经完成的部分

### 1. Agent 服务已经独立成型

当前仓库已经有独立的 `agent-service`，并且以 FastAPI 形式提供内部接口，能够接收后端下发的 run 执行请求。

它目前已经具备：

- 接收 backend 的内部执行请求
- 拉取 run context、模型配置、文件上下文、workspace memory
- 选择运行引擎
- 执行 Agent 主流程
- 将事件、结果、失败状态回写给 backend

这说明 Agent 已经不是“概念设计”阶段，而是进入了**可联调、可运行**阶段。

### 2. Agent 主流程已经跑通

当前默认路径是 LangGraph 版本的 universal agent graph，已经具备以下主链路：

1. 接收用户问题
2. 做意图判断
3. 判断是闲聊、澄清、工具调用还是暂不支持
4. 如果命中工具，先选择工具
5. 根据工具偏好决定是否自动调用或要求用户确认
6. 工具执行后产出结果摘要
7. 生成最终回复
8. 回写 run 状态与事件

从工程角度看，这条链已经完整闭环。

### 3. 管理端和用户端已经接入 Agent

目前前后端联调已经不是纸面状态：

- 用户端已经有 Agent 页面
- 管理端已经有模型配置能力
- backend 已经有 Agent 相关表、会话、run、事件、工具确认、workspace memory 等接口
- agent-service 已经能读取这些上下文并执行

也就是说，**产品形态已经可见**，不是只有后端代码。

### 4. 模型配置链路已经基本打通

现在 Agent 使用的模型并不是硬编码死的，已经能通过 backend 的 active model config 拉取到：

- provider
- model name
- base URL
- API Key
- timeout

这意味着 Agent 至少在“模型接入层”已经具备后续扩展能力。

### 5. 预算、安全、确认机制已经有第一版

当前已经能看到比较完整的 Agent 安全和成本边界：

- PromptGuard：做基本的提示词安全拦截
- BudgetGuard：控制模型调用次数、工具调用次数和预算
- Tool confirmation：首次调用工具前要求用户确认
- Tool preference：支持自动调用偏好
- 内部 API 签名校验：保护 agent-service 的内网调用

这说明项目不是只追求“先跑起来”，而是已经在往“可控的 Agent”方向做。

## 目前还没做实的部分

### 1. 工具执行仍然偏“占位实现”

这是当前最核心的问题。

虽然 Agent 已经有“选工具 -> 确认工具 -> 执行工具 -> 总结结果”的完整流程，但在 `agent-service` 这一层里，工具执行目前更接近：

- 创建一条 tool call 记录
- 发出 `tool.started`
- 返回一段占位性质的 summary
- 发出 `tool.finished`

也就是说，**Agent 当前最像是在“模拟调用工具”，而不是在 Agent 主循环里真正拿到工具的真实业务结果后再继续推理。**

这是完成度还不能算高的最主要原因。

### 2. 意图识别还比较初级

当前 intent router 主要是：

- 关键词判断
- 特定短语判断
- 文本长度判断

例如“你是谁”这种短问题，会因为长度过短被判定成“需要补充信息”，而不是正常闲聊。

这说明当前的意图识别还属于**规则驱动的第一版**，离“自然、稳健、泛化能力强”的 Agent 还有明显距离。

### 3. Tool matching 还比较粗糙

当前工具选择更多是基于：

- tool code
- tool name
- description
- hints
- 预设关键词

做字符串层面的匹配。

这类实现的优点是简单、可控、便于联调；缺点是：

- 容易误匹配
- 多工具场景下不够稳
- 不擅长复杂语义
- 缺少真正的“选择理由”

因此当前的 Agent 更像“工具路由器 + 对话包装层”，还不是“成熟的工具型智能体”。

### 4. 流式输出还不是真正的流式

目前用户端看到的 `message.delta` 更像是：

- 模型一次性生成完整文本
- 服务端再按固定长度切片回放

这能让前端看起来像“流式输出”，但本质上不是 token-level streaming。

从产品体验上已经够用，但从技术完成度上看，还属于“先把前端效果做出来”的阶段。

### 5. Deep Agents 路线还没有真正启用

仓库里已经有 deep agents engine、subagent profiles、subagent trace 等结构，但默认并没有打开，主路径仍然是 LangGraph 版本。

因此目前的子代理、多阶段计划、复杂任务分解，更像是**预留能力**，而不是当前主功能。

## 当前阶段更准确的产品定位

如果要给目前的 Agent 一个准确定位，我会更倾向于这样描述：

> 它现在已经是一个“能跑通主链路的 Agent Alpha 版本”，而不是一个“真正成熟的、可稳定交付复杂任务的生产级 Agent”。

更具体一点：

- **不是** PPT 式概念设计
- **不是** 只有接口定义没有运行链路
- **不是** 纯 Demo 页面
- **而是** 已经具备真实工程骨架、能联调、能运行、能演示用户流程的第一阶段成品

但同时它也还不是：

- 真正稳定的多工具执行 Agent
- 强语义理解的智能路由器
- 生产级可观测、可扩展、可回溯的复杂智能体系统

## 当前最值得优先推进的改进方向

### 1. 先把“真工具执行”彻底打通

这是优先级最高的一项。

如果 Agent 还只是停留在“选择了工具，但实际工具结果是占位 summary”，那整个 Agent 的价值会被明显削弱。应尽快让 Agent 真正接到工具的真实执行结果，再进行总结与继续推理。

### 2. 升级意图识别与工具选择

建议从当前的纯规则策略，升级为“规则 + 模型判断”的混合策略：

- 简单请求继续走规则，保证低成本
- 模糊请求交给模型判断，提高准确率
- 多工具命中时引入打分、重排或解释机制

### 3. 让 Agent 输出真正流式化

这会直接提升用户体验，也能让 Agent 更像一个实时助手，而不是“等一下再整段吐出来”。

### 4. 明确 LangGraph 与 Deep Agents 的分工

现在两条路线都在仓库里，但主次关系和启用条件还不够清晰。后续需要明确：

- 什么场景走轻量 LangGraph
- 什么场景走 deep agents
- 二者是否长期并存
- 是否需要做 per-run runtime 选择

### 5. 补足观测与调试能力

当前事件系统已经有基础，但仍建议继续增强：

- 每一步决策为什么发生
- 为什么选这个工具
- 为什么需要澄清
- 为什么失败
- 模型与工具各消耗多少预算

这些信息一旦补齐，后续优化 Agent 会轻松很多。

## 结论

当前 Agent 的状态可以概括为：

> **架构已经搭起来，主链路已经跑通，产品形态已经成型，但核心智能体能力仍处于第一阶段。**

因此，当前完成度最合适的判断是：

- **工程骨架完成度：60% 到 70%**
- **真实产品能力完成度：40% 到 50%**

从项目节奏上看，这已经是一个不错的阶段性成果。下一步不应该再继续堆页面或堆新概念，而应该优先把“真实工具执行、意图识别、流式体验、可观测性”几件核心能力做扎实。

## 架构上不合理或后期容易出问题的地方

下面这些问题，不一定会在当前阶段立刻把系统跑挂，但随着 Agent 能力继续增加、工具数量变多、用户规模变大，它们大概率会逐步变成真正的架构瓶颈。

### 1. Agent 的“工具执行层”和真实工具系统是脱节的

这是当前最核心的架构问题。

现在 `agent-service` 里的工具执行，并不是直接把 Agent 选中的工具真正跑起来，而更像是：

- 记一条 tool call
- 发事件
- 返回一个占位 summary

这意味着当前 Agent 主循环里的“执行工具”与实际的 AI 工具运行链路（backend + queue + worker）并不是一个统一闭环。

这会带来几个后期问题：

- Agent 看到的工具结果不一定是真实业务结果
- 工具链路会出现“双系统”：一个是普通 AI 工具运行链路，一个是 Agent 内部工具链路
- 后续如果想让 Agent 调用已有 AI 工具，就容易出现“同一个工具被实现两套执行逻辑”
- 调试时很难判断问题到底出在 Agent 侧、backend 侧还是 worker 侧

如果这个问题不尽早统一，后面工具越多，系统越容易变成一团交叉耦合的“补丁式架构”。

### 2. 意图识别是规则写死的，扩展性很差

当前 intent router 主要依赖：

- 文本长度
- 关键词
- 固定短语

这类方式在第一阶段联调非常高效，但从架构上看，它的问题是：

- 每加一种新能力，就要继续堆 if/else 和关键词
- 很难解释复杂请求
- 容易因为边界条件产生误判
- 中文、英文、混合表达、口语化表达的鲁棒性不足

更严重的是，这类规则会逐渐变成**隐性产品逻辑**。当项目继续扩展时，开发者可能已经说不清“为什么这句话会被判成这个意图”，维护成本会越来越高。

### 3. Tool matching 机制过于脆弱，后期工具一多就容易失控

当前工具匹配基本是基于：

- tool code
- tool name
- description
- hints
- 关键词的字符串匹配

这在工具数量较少时还能接受，但后面工具一多，会很容易出现：

- 多个工具同时命中
- 一个工具误命中另一个工具的关键词
- 小改一段 description 就影响命中结果
- 排查为什么选错工具会非常困难

也就是说，当前 ToolRegistry 更像是“字符串搜索器”，还不是一个可靠的工具路由系统。

### 4. LangGraph 路径和 Deep Agents 路径长期并存，容易造成双轨维护成本

目前仓库里同时存在：

- LangGraph 版本的 universal agent graph
- Deep Agents runtime

这在探索阶段是合理的，但如果长期这样发展，而没有明确主次关系，后面会很容易出问题：

- 两套运行时行为不一致
- 两套事件语义不完全一致
- 两套调试方式不同
- 一个修了 bug，另一个忘了同步
- 文档、前端、后端对“Agent 到底怎么跑”的认知会逐渐分裂

简单说，**双 runtime 不是不能有，但必须明确谁是主线，谁是实验线。**

否则后期团队会把大量时间浪费在“同步两套系统行为”上。

### 5. 运行时选择是全局开关，而不是 per-run 决策

现在 Deep Agents 是否启用，更多是一个全局配置项，而不是根据：

- 当前用户请求类型
- 工具复杂度
- 会话阶段
- 成本预算
- 失败回退策略

来做单次 run 的动态选择。

这个设计在当前阶段简单直接，但后面会变成一个问题：
**系统无法做“按请求类型选择最合适 runtime”的调度。**

结果就是：

- 要么全量走重路径，成本高、性能差
- 要么全量走轻路径，能力不够
- 很难做渐进式上线

从长期架构上看，runtime 选择更适合做成**run 级策略决策**，而不是全局开关。

### 6. 事件系统虽然有了，但业务语义还不够统一

当前 Agent 已经有不少事件：

- intent.detected
- tool.selected
- tool.confirmation_required
- tool.started
- tool.finished
- message.delta
- run.failed

这很好，但从架构角度看仍有潜在问题：

- 有些事件只在一条 runtime 路径里出现
- 有些事件定义了但实际并没有稳定发出
- run.started / run.completed / run.failed 的责任边界在 backend 和 agent-service 之间还不够清晰
- 事件更偏“前端展示用途”，还没有完全成为“统一调试与回溯协议”

短期内这不一定明显，但后面如果要做：

- 运行回放
- 问题定位
- 成本统计
- 质量评估
- Agent 行为分析

就会发现事件体系还不够稳。

### 7. 模型流式能力是伪流式，后面会限制体验与能力扩展

当前 `message.delta` 更像是把完整答案切片后再往前端发送，而不是真正的 token streaming。

这个设计短期内可以满足产品演示，但后面会出现几个限制：

- 用户看到的是“假流式”，长回答还是需要整段生成完成后才能开始回放
- 很难做真正的实时中断、实时取消、实时工具插入
- 如果以后要做更复杂的多轮计划过程展示，就会很吃力

也就是说，现在的流式协议更偏向 UI 效果，不是完整的 runtime 能力。

### 8. Agent 的核心逻辑集中在 Python 服务，但系统状态主权在 Java backend，边界容易模糊

现在的架构里：

- run、session、tool call、credit、workspace memory 等核心状态主要在 backend
- 但 agent 的决策、工具编排、模型调用、部分事件又在 `agent-service`

这种拆分本身没问题，但如果边界定义不清晰，后面容易出现：

- 谁负责最终状态推进不清楚
- 谁是 run 生命周期的唯一事实来源不清楚
- backend 和 agent-service 之间出现重复逻辑
- 某些校验做两遍，某些校验又没人做

这类问题通常不是一开始就爆，而是系统做大之后，大家会越来越难判断“某个行为到底应该改 backend 还是改 agent-service”。

### 9. 当前更像“状态机 + 工具路由器”，还不是完整的任务规划架构

现在的 LangGraph 路径本质上是：

- 识别意图
- 选工具
- 确认
- 执行
- 总结

这非常适合作为第一阶段落地，但它也意味着当前架构更偏向：

- 单步决策
- 单工具调用
- 短链路流程

如果未来要支持：

- 多步计划
- 多工具串联
- 条件分支
- 中间结果缓存
- 自动重试与回退

那现有结构会逐渐显得不够。到那时如果继续在当前逻辑上硬叠功能，就容易把简单状态机逐步改成难以维护的“巨型分支图”。

### 10. 缺少一个真正清晰的“统一 Agent 契约层”

目前系统里已经有很多层：

- frontend
- backend
- agent-service
- worker
- tool definitions
- events
- run context
- tool call records

但严格来说，还缺少一个稳定且统一的“Agent 契约层”来定义：

- 一个 Agent run 的标准输入是什么
- 一个工具调用的标准结果是什么
- 一个运行中的中间态应该如何表达
- 失败、取消、确认、重试的统一语义是什么

现在这些信息散落在多个模块里，短期内问题不大，但后面随着能力继续扩张，**契约不统一**会是最容易带来系统性返工的问题。

## 哪几个问题最应该优先处理

如果要从架构角度挑优先级最高的几个问题，我会排成下面这个顺序：

### 第一优先级

- 工具执行链路统一，避免 Agent 工具流和普通工具流长期双轨
- 明确 backend 与 agent-service 的职责边界
- 统一 Agent run / tool call / event 的契约语义

### 第二优先级

- 升级意图识别与工具选择机制
- 明确 LangGraph 与 Deep Agents 的主次关系
- 支持 per-run runtime 选择

### 第三优先级

- 真正流式输出
- 更完整的观测、诊断和回放体系
- 为未来多步规划、多工具编排预留更清晰的执行模型

## 总结

从架构角度看，当前 Agent 最大的问题不是“代码写得少”，而是：

> **主链路已经搭起来了，但若不尽快统一执行链路、职责边界和契约层，后续很容易从“能跑的 Agent”演变成“越来越难维护的多层补丁系统”。**

所以后面最重要的事情，不是立刻继续往上堆更多花哨能力，而是先把底层结构做稳，尤其是：

- 工具执行统一
- runtime 选择清晰
- backend / agent-service 边界稳定
- 事件与状态契约统一

这些事情做好之后，后续无论是接入更多工具、做更复杂的多步 Agent、还是引入真正的子代理体系，成本都会低很多。

## 稳定性、异常处理、可观测性还缺什么

这一部分和“功能有没有做出来”不是一回事。

很多系统在前期看起来已经能跑，但一旦开始真实多人使用、长时间运行、频繁失败重试、跨服务联调，就会暴露出另外一类问题：

- 不够稳
- 出问题时不容易恢复
- 出了问题定位很慢
- 同样的问题反复发生

从当前 Agent 的状态来看，稳定性、异常处理、可观测性已经有了第一层基础，但距离真正工程化可控，还缺不少关键能力。

## 一、稳定性方面还缺什么

### 1. 缺少更明确的超时、取消、回收策略

当前系统里虽然已经有超时、失败、budget guard 等概念，但从整体架构上看，还缺少一套更完整的运行时控制策略：

- 一个 run 最长允许跑多久
- 一个工具调用最长允许卡多久
- 用户关闭页面后 run 是否继续
- backend 和 agent-service 断连时，run 应该如何收敛
- agent-service 重启时，正在执行的 run 如何恢复或补偿

如果这些策略不明确，后面很容易出现：

- run 长时间卡在中间状态
- 用户以为失败了，后台其实还在跑
- backend 认为 run 已结束，但 agent-service 还在继续发事件
- worker / tool call 已经完成，但前端没有收到正确收敛状态

也就是说，现在系统更像“能跑”，但还不完全是“能稳定收尾”。

### 2. 缺少真正的幂等保障

Agent 系统天然会遇到重试：

- HTTP 重试
- 页面重复点击
- backend 重发内部请求
- agent-service 因网络波动重复写事件
- tool call 在不确定状态下重复提交

如果没有更严格的幂等策略，后面会出现：

- 同一条用户消息触发两次 run
- 同一个 run 被重复执行
- 同一个工具被重复确认或重复调用
- 事件重复写入，前端 timeline 混乱
- credits 冻结 / 扣减重复发生

当前系统已经有一些 runId / toolCallId 等主键，但从工程角度看，还需要更系统地定义：

- 哪些接口天然幂等
- 哪些接口必须带 request id
- 哪些事件允许重复、哪些必须去重
- 重试时谁负责判断“已经做过”

否则稳定性问题会逐步转化成数据一致性问题。

### 3. 缺少对跨服务失败的系统性补偿

这个 Agent 不是单体应用，而是至少跨了：

- frontend
- backend
- agent-service
- model provider
- 可能的 worker / 工具执行链路

因此真正的失败往往不是“某个函数抛异常”，而是：

- backend 已创建 run，但 agent-service 没真正启动
- agent-service 已调用模型，但回写结果时 backend 失败
- tool call 已落库，但工具实际没有执行
- 前端超时，但后台其实成功了

现在系统已经有 `run.failed`、内部事件、异常回写等机制，但从长期看还缺：

- 明确的补偿动作
- 明确的重试边界
- 失败状态的最终一致性策略
- “部分成功”时的恢复逻辑

否则后续只要链路稍微复杂一点，就会出现很多“半成功半失败”的脏状态。

### 4. 缺少更强的背压与流量保护能力

Agent 是一个高成本链路：

- 要读上下文
- 要调模型
- 可能要调工具
- 要持续写事件

如果并发上来，仅靠简单的 rate limit 和 budget guard 往往不够。还需要考虑：

- 队列积压时怎么处理
- agent-service CPU / IO 打满时怎么降级
- 第三方模型接口抖动时怎么限流
- 某个工具异常高频调用时怎么熔断
- 某个用户连续触发失败 run 时怎么快速止损

如果缺少这类能力，系统会出现“不是单个请求失败，而是整批请求都变慢或全线抖动”的问题。

## 二、异常处理方面还缺什么

### 1. 现在更多是“接住异常”，还不是“分类处理异常”

当前很多地方已经有 try/catch 或统一失败回写，这很好，但更进一步还需要区分异常类别。

比如：

- 用户输入问题
- 工具缺参
- 模型调用超时
- 模型服务返回 4xx
- 模型服务返回 5xx
- backend 内部错误
- 网络抖动
- 事件写入失败
- 业务状态冲突

这些异常虽然都可以表现成“运行失败”，但在处理上不应该一样：

- 有的应该提示用户补充信息
- 有的应该自动重试
- 有的应该立即终止
- 有的应该只记录日志，不中断主流程
- 有的应该触发熔断或降级

如果异常分类做得不够细，后面前端看到的就会是大量模糊的“系统异常”，但研发又不知道该优先修哪里。

### 2. 缺少更明确的错误码体系和分层语义

现在系统已经有部分错误码，但从长期来看，还需要把错误码彻底分层：

- 面向用户展示的错误
- 面向前端逻辑判断的错误
- 面向服务治理的错误
- 面向排障的内部错误

否则很容易出现一个问题：
**同一个错误码同时承担“用户提示 + 业务判断 + 排障诊断”三种职责。**

这会让错误语义逐渐混乱，后面修改提示文案或处理逻辑时非常容易牵一发动全身。

### 3. 缺少明确的降级策略

Agent 未来一定会遇到很多“部分能力暂时不可用”的情况，比如：

- 模型服务暂时不稳定
- 某个工具故障
- Deep Agents 路径异常
- workspace memory 检索失败
- 文件解析失败

这时候系统不应该只有“成功”或“整体失败”两种结果，还应该有：

- 回退到简单聊天模式
- 跳过某个可选步骤继续回答
- 不用 memory 继续跑
- 工具失败但仍给出部分结论
- 使用备用模型或默认模型

如果没有降级策略，系统的可用性会非常脆弱，一旦某个环节出问题就整条链路报错。

### 4. 缺少统一的“异常到用户提示”映射策略

现在很多错误最后会变成前端上的：

- 系统异常
- Agent 暂时无法继续
- 模型调用失败

这些提示虽然对联调阶段够用，但对正式使用来说还不够。

后续需要一套统一规则来回答：

- 用户到底应该看到多详细的错误
- 哪些错误应该提示“稍后重试”
- 哪些错误应该提示“补充输入”
- 哪些错误应该提示“联系管理员”
- 哪些错误应该完全隐藏内部细节

否则用户体验会非常不稳定，同一种问题可能在不同页面显示出不同风格的错误信息。

## 三、可观测性方面还缺什么

### 1. 现在有事件，但还没有形成完整的诊断闭环

当前 Agent 已经在发很多事件，这很好，但真正可观测性需要回答的不只是“发生了什么”，还包括：

- 为什么发生
- 花了多久
- 消耗了多少资源
- 下一步为什么这样决策
- 哪一步最容易失败

从当前状态看，事件系统更偏向前端时间线和基本回写，还不足以支撑真正高效的排障。

### 2. 缺少统一 trace / span 视角

后面如果要排查一个 Agent 问题，理想状态应该能看到完整链路：

1. 用户发起请求
2. backend 创建 run
3. agent-service 拉取上下文
4. intent 分类
5. 工具选择 / 确认
6. 模型调用
7. 工具调用
8. 结果回写
9. 前端收到消息

但如果没有统一 trace id / span 体系，这些日志和事件就会散落在多个系统里，排查时非常痛苦。

这类问题在开发环境里不明显，但一到线上、多请求并发时，几乎一定会成为效率瓶颈。

### 3. 缺少关键指标（Metrics）体系

真正的可观测性不能只靠日志，还需要指标。至少后面应该能持续看到：

- run 总量
- 成功率 / 失败率
- 平均耗时 / P95 / P99
- 模型调用成功率
- 工具调用成功率
- 确认率 / 自动调用率
- 失败原因分布
- 每类工具的平均耗时
- 每个模型的耗时和错误率
- 每个 runtime 的耗时和稳定性

如果没有这些指标，团队就很难判断：

- 系统到底是在变好还是变差
- 哪一类问题最严重
- 哪个模型配置最不稳定
- 哪个工具最容易出错

### 4. 缺少结构化日志约束

当前日志足够看基本问题，但从长期治理角度看，更理想的是每类关键日志都结构化，并统一包含：

- traceId
- runId
- sessionId
- userId
- toolCode
- runtime
- modelProvider
- modelName
- errorCode
- elapsedMs

如果没有这个规范，后面日志会越来越多，但信息会越来越散，不利于检索和聚合分析。

### 5. 缺少“决策可解释性”记录

Agent 系统和普通 CRUD 系统不一样，排障时经常要回答：

- 为什么被判成这个 intent
- 为什么选了这个工具
- 为什么要求确认
- 为什么没有走 deep agents
- 为什么命中了某条 prompt guard

这些信息如果只存在于代码逻辑里，而没有被显式记录成事件或日志，后面每次排查都只能重新读代码猜原因。

这会让系统越来越“黑盒”。

### 6. 缺少运行回放能力

一个成熟的 Agent 系统，后面通常都需要支持某种程度的 run replay / debug replay：

- 复现一次失败请求
- 复现某个模型输出
- 看当时的上下文、memory、tool selection
- 还原用户看到的完整过程

当前系统已经有 run、message、event、tool call 这些基础对象，这是好事，但距离“可回放”还差：

- 完整上下文快照
- 关键决策快照
- 版本信息（模型、提示词、工具定义）
- 同步的链路时间线

如果这部分后面不补，系统复杂起来之后，很多线上问题会“看得见出错，看不见为什么出错”。

## 四、如果从工程角度排序，最该先补哪些

如果只从稳定性、异常处理、可观测性三个维度选最优先的，我会建议按下面顺序补：

### 第一优先级

- 明确 run / tool call / event 的最终一致性与补偿策略
- 补齐超时、取消、重试、幂等规则
- 统一错误码和错误分层语义

### 第二优先级

- 建立统一 trace id 链路
- 增加关键 metrics
- 补结构化日志规范

### 第三优先级

- 增强降级策略
- 增加决策可解释性记录
- 为运行回放准备上下文快照与版本信息

## 总结

当前 Agent 在稳定性、异常处理、可观测性方面，已经有了“能支撑开发联调”的基础，但还没有达到“能长期稳定演进和快速排障”的工程化水平。

更准确地说：

- **稳定性基础已具备，但缺少系统性收敛策略**
- **异常处理已能兜底，但缺少细粒度分类与降级能力**
- **事件与日志已经存在，但离完整可观测体系还有明显距离**

如果后面继续增强 Agent 能力，而不先补这些基础设施，最终最容易出现的情况不是“功能做不出来”，而是：

> **功能越做越多，问题越难定位，系统越跑越像黑盒。**

所以从工程角度看，这一块的重要性并不比“新功能开发”低，甚至在下一阶段应该优先级非常高。

## 8. 分阶段推进计划

下面给出一个更适合当前状态的分阶段推进方案。这个计划不是按“功能想象空间”来排，而是按**对当前 Agent 成败影响最大**的顺序来排。

总体原则是：

1. 先把核心执行链路做实
2. 再把稳定性和可观测性补齐
3. 再升级智能决策能力
4. 最后再扩展更复杂的 Agent 形态

---

### 第一阶段：打通真实工具执行闭环

#### 目标

让 Agent 从“会选工具、会确认工具、会生成工具调用事件”升级为：

> **真的能够调用真实工具，并基于真实结果继续回复。**

这是当前最关键的一步。只要这一步没有做实，Agent 的整体价值就还是偏 Demo / Alpha。

#### 改动点

- 统一 Agent 工具调用链路与普通 AI 工具执行链路
- 明确 tool call 生成后，究竟由谁真正触发执行
- 让 Agent 能拿到真实工具执行结果，而不是占位 summary
- 统一 tool result 的数据结构
- 明确 Agent 调用工具后的同步 / 异步回传方式
- 如果工具是异步执行，补足 Agent 等待结果、超时、失败回写的机制

#### 风险

- backend、agent-service、worker 三方边界可能需要重新梳理
- 一旦执行链路统一，旧的 stub 流程会暴露出大量兼容性问题
- 异步工具执行与 Agent 回复时序可能变复杂
- credits、tool call 状态、run 状态三者之间可能出现一致性问题

#### 验证方法

- 选 1 到 2 个最典型工具（例如小红书文案、商品标题优化）做端到端打通
- 验证 Agent 选中工具后，真实工具确实执行，并能拿到真实结果
- 验证工具成功、失败、超时三种路径下，run 状态都能正确收敛
- 验证前端 timeline、message、tool call 记录三者一致
- 验证 credits 冻结、释放、结算逻辑正确

---

### 第二阶段：补齐运行稳定性和状态一致性

#### 目标

让 Agent 从“能跑通”升级为：

> **遇到错误也能稳定收尾，不容易留下脏状态。**

这一阶段的重点不是做更多能力，而是确保已有能力不会在多人联调和频繁异常下失控。

#### 改动点

- 定义 run、tool call、event 的幂等规则
- 明确超时、取消、重试、补偿策略
- 统一 backend 与 agent-service 在状态推进上的职责
- 增加对“部分成功 / 部分失败”场景的处理逻辑
- 补足工具确认流程中的竞态保护
- 增加跨服务调用失败后的最终一致性收敛策略

#### 风险

- 这一阶段会牵涉很多状态机边界，修改容易带来隐藏回归
- 如果之前 run / tool call / event 的职责划分不够清晰，会需要一定重构
- 可能会发现一些现在已经存在但还没暴露的数据一致性问题

#### 验证方法

- 人工模拟重复提交、超时、取消、接口重试、服务重启
- 验证同一 run 不会被重复执行
- 验证同一 tool call 不会被重复结算或重复确认
- 验证异常时 run 最终一定进入可解释的终态
- 增加一组状态机回归测试，覆盖成功 / 失败 / 取消 / 超时 / 重试路径

---

### 第三阶段：补齐可观测性、诊断能力和运维基础

#### 目标

让 Agent 从“出了问题靠读日志猜”升级为：

> **出了问题可以快速定位、快速复现、快速判断影响范围。**

#### 改动点

- 建立统一 trace id 链路，贯通 frontend、backend、agent-service、worker
- 增加关键指标：run 成功率、耗时、失败原因、模型错误率、工具错误率等
- 统一结构化日志字段
- 增加关键决策日志：为什么判这个 intent、为什么选这个工具、为什么需要确认
- 统一错误码分层和用户提示映射
- 为后续 run replay / 调试回放预留上下文快照

#### 风险

- 如果可观测性埋点不收敛，容易越埋越乱
- 日志和事件过多会增加存储与检索成本
- 如果 trace 规范没有一次性定好，后面回补成本会很高

#### 验证方法

- 选一条失败 run，要求能在 5 到 10 分钟内从日志和事件里定位根因
- 选一条成功 run，要求能完整看到主要执行路径、耗时和关键决策
- 验证 dashboard 或基础统计能反映成功率、失败率、平均耗时
- 验证同一 traceId 能串起多服务日志

---

### 第四阶段：升级意图识别与工具路由能力

#### 目标

让 Agent 从“规则驱动的路由器”升级为：

> **更自然、更稳健、更能理解复杂用户意图的智能路由系统。**

#### 改动点

- 把纯规则 intent router 升级为“规则 + 模型判断”的混合模式
- 收缩或移除过强的长度阈值、关键词硬判断
- 为工具选择增加打分、候选排序和冲突消解机制
- 增加澄清问题生成能力，而不是只返回固定提示语
- 提高一般闲聊、短问句、模糊表达的处理质量

#### 风险

- 引入模型判断后，成本和延迟会上升
- 如果提示词和评分机制设计不好，可能会造成工具命中不稳定
- 规则与模型混合时，容易出现“谁说了算”不清晰的问题

#### 验证方法

- 建立一组典型用户请求样本，覆盖闲聊、模糊请求、单工具请求、多工具歧义请求
- 对比升级前后的正确率
- 验证“你是谁”“帮我处理一下”“给我一个方案”这类短消息的表现是否更合理
- 验证多工具场景下的误选率是否下降

---

### 第五阶段：升级输出体验与交互质量

#### 目标

让 Agent 从“流程上能工作”升级为：

> **在用户体验上更像一个实时、自然、可信的助手。**

#### 改动点

- 支持真正的流式输出，而不是结果切片伪流式
- 优化工具确认交互和等待态展示
- 更清晰地区分：正在思考、正在调工具、正在等待结果、已完成
- 统一前端错误提示风格
- 优化 run timeline 展示，让用户更容易理解 Agent 在做什么

#### 风险

- 真流式会增加前后端协议复杂度
- 一旦前端状态管理不稳，流式交互容易引入新竞态问题
- 更丰富的事件展示可能反过来暴露后端事件语义不统一的问题

#### 验证方法

- 人工验证长回复场景下的实时性体验
- 验证工具确认、取消、失败时前端状态是否清晰
- 验证在弱网 / 断流场景下是否能平滑回退
- 让非开发同学试用，看是否能自然理解 Agent 当前处于什么状态

---

### 第六阶段：明确 runtime 战略并引入更复杂的规划能力

#### 目标

让系统从“只有一个主流程的 Agent”升级为：

> **能够根据请求复杂度选择不同执行策略，并逐步支持多步规划、多工具协作。**

#### 改动点

- 明确 LangGraph 路线和 Deep Agents 路线的主次关系
- 把 runtime 选择从全局开关升级为 per-run 策略选择
- 为复杂任务引入多步计划、阶段性结果、失败回退策略
- 如果需要，逐步引入真正的 subagent 机制，而不是只停留在 hint 层
- 为多工具串联执行预留更清晰的调度模型

#### 风险

- 这一阶段会显著提升系统复杂度
- 如果前面契约层和状态一致性没做好，这一步会非常痛苦
- 多 runtime 并存容易再次带来双轨维护问题

#### 验证方法

- 定义简单请求与复杂请求两类样本，验证能否自动选择合适 runtime
- 验证复杂任务是否能分阶段完成，而不是一次性失败
- 验证多步任务中的中间状态、预算消耗、错误处理是否可解释
- 验证新增复杂能力后，不会破坏原有简单场景的稳定性

---

### 第七阶段：沉淀平台化能力

#### 目标

让 Agent 从“项目里的一个功能模块”升级为：

> **一个可以持续扩展、持续治理、持续评估的 Agent 平台能力。**

#### 改动点

- 统一 Agent 契约层
- 沉淀 prompt / tool / runtime / policy / event 的版本化管理
- 引入 run replay、效果评估、数据分析
- 为后续更多业务接入同一 Agent 平台预留接口
- 建立稳定的测试基线与评估样本集

#### 风险

- 平台化会带来前期投入增加
- 如果基础能力还没稳，过早平台化反而会放大复杂度
- 需要团队在命名、契约、边界上形成统一认知

#### 验证方法

- 新增一个工具或一个能力时，接入成本明显下降
- 同一套 Agent 基础设施可以被多个场景复用
- 出问题时能通过回放、日志、指标快速定位
- 团队成员能比较清晰地回答“某类问题应该改哪一层”

---

## 推荐的实际推进顺序

如果只考虑当前项目最现实、最有效的推进顺序，我建议：

1. **第一阶段：真实工具执行闭环**
2. **第二阶段：稳定性和状态一致性**
3. **第三阶段：可观测性和诊断能力**
4. **第四阶段：意图识别与工具路由升级**
5. **第五阶段：交互体验升级**
6. **第六阶段：runtime 战略与复杂规划能力**
7. **第七阶段：平台化沉淀**

## 最后总结

这套分阶段计划的核心思想是：

> **先把 Agent 做“真”，再把 Agent 做“稳”，再把 Agent 做“聪明”，最后把 Agent 做“强”。**

如果顺序反过来，比如在真实工具执行和稳定性都没做实之前，就先大规模上复杂规划、多 runtime、多子代理，很容易把系统拖进高复杂度但低可用性的状态。

所以从当前项目情况看，最理性的推进方式不是追求“最快做出最复杂的 Agent”，而是：

> **每一阶段都解决当前最关键的工程矛盾，并为下一阶段留出稳定的结构基础。**
