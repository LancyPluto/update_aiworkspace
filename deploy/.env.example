# Docker Hub 镜像加速前缀（国内开发默认 docker.1ms.run/；直连官方 Hub 请设为空）
DOCKER_REGISTRY=docker.1ms.run/
APT_MIRROR=mirrors.aliyun.com

# 容器出站代理：生产 mihomo 7890；本地 Clash TUN/fake-ip 开发必须配置（否则豆包等外连探测 SSL 失败）
# 复制为 deploy/.env 后取消注释，端口与 Clash「混合代理」一致（常见 7897 或 7890）
HTTP_PROXY=http://host.docker.internal:7897
HTTPS_PROXY=http://host.docker.internal:7897
NO_PROXY=localhost,127.0.0.1,mysql,redis,rabbitmq,backend,agent-service,admin-frontend,user-web,nginx,host.docker.internal
# 生产可追加：wlcloudai.com,8.134.93.203,.aliyuncs.com,.aliyun.com
