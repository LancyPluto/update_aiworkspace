# 文档上传分支变更记录

## 2026-06-06

### 音乐生成与多模态扩展

- 新增 `MUSIC_GENERATION` 工具类型与 Suno Worker 链路（`suno_music_client`、`music_generation_handler`）。
- 后端 `model_provider_metadata` 表与执行快照下发；社区帖子 `media_url` 支持音频展示。
- User Web：工作台能力控件、字段 UI 分层、音频结果渲染；Admin：字段 schema 与用户预览卡。

### 基线

- 已合并 `origin/dev`（支付宝、充值、部署脚本等）。

### 不上传

- `.env`、`data/generated-media/**`、`backend/data/agent-files/**`。

### 验证

- `pytest worker/tests/test_suno_music_client.py worker/tests/test_music_generation_handler.py`

## 2026-06-05

### Worker：oFox 参考图生图

- 修复 `openai_images` 参考图走 `/images/edits` 时网关报「缺 model」：Session 默认 `Content-Type: application/json` 污染 multipart 请求。
- 新增 `worker/utils/input_image.py` 统一解析参考图 URL / 本地路径 / base64。
- 补充单测与 `docs/工作区整理与上传-2026-06-05.md`。

### User Web：工作台重试

- `retryTask` 改用 `buildOptimisticDashboardTask`，修复重试后列表不刷新。

### Agent / Admin

- Agent 记忆整理与 deep agents 运行时小步调整；管理端 agent-runs 与统一 API 设置页更新。

### 不上传

- `.env`、`data/generated-media/**` 等本地产物。

### 验证

- `pytest worker/tests/test_openai_images_client.py worker/tests/test_input_image.py`

## 2026-05-29

### 用户端视觉与交互优化

- 将用户端整体视觉继续向 SeaArt 风格收敛：深色模式、玻璃拟态、柔和紫色高光、悬浮输入框和更清爽的资产预览。
- 优化 `AppShell` 顶部栏：搜索框与算力展示解耦，新增独立“剩余算力”胶囊，创建按钮改为更柔和的渐变与阴影。
- 增加用户名显示兜底逻辑：当昵称疑似出现编码乱码时，回退展示用户名或可读前缀。

### 工作台与素材资产

- 工作台与素材库统一使用资产预览弹窗，支持图片、视频、音频和文本结果的沉浸式查看。
- 资产弹窗右侧提供可继续创作的工具推荐，支持将当前资产带入工作台继续生成。
- 工作台历史任务支持模态过滤、下拉动态加载、生成中状态前置展示、失败重试和失败/取消任务删除。
- 工作台创建浮窗、素材选择弹窗、模态浮窗和历史任务卡片已按 SeaArt 风格做轻量化与位置修正。

### Agent 页面

- 移除 Agent 右侧空的“工作区记忆”面板，让对话区获得更完整的可用宽度。
- 优化 Agent 对话 UI：更有呼吸感的气泡、生成中状态、紧凑模型选择器和简洁多媒体结果展示。
- Agent 历史会话增加“今天 / 昨天 / 前 7 天 / 更早”分组，长标题单行省略，删除按钮仅在悬停时显示。
- Agent 输入框禁用浏览器默认拖拽缩放，改为自动撑高并限制最大高度。
- Agent 生成的图片、视频、音频结果点击后接入统一资产卡片，不再暴露后台 `provider/model` 等配置字段。

### 结果解析与详情页

- `/tasks/:id/result` 改为复用统一的 `buildTaskResultBlocks` 结果解析逻辑，避免 Agent 任务和工作台任务展示不一致。
- 修复 Agent 结构化返回内容中完整 JSON 被误当作提示词展示的问题，只提取真实用户输入字段。
- 视频模型效果和任务结果中的媒体 URL 统一归一化，减少本地与线上环境展示差异。

### 验证

- `npm run build` in `user-web`

## 注意事项

- 当前工作区存在若干未纳入本次提交的本地运行产物，例如 `backend/data/agent-files/**` 和 `.env`，这些不应随前端 UI 优化一起提交。
- 分支当前为 `feature/admin-web`，本次提交只包含用户端前端优化与对应文档。
