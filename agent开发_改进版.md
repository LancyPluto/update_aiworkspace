# Agent 改进版开发计划

## 1. 当前判断

当前 Agent 已经具备基础工程骨架和主流程雏形：前端页面、后端 run / event / tool call 相关接口、agent-service、模型配置、确认流和部分预算控制都已经存在。

但从产品能力看，它还不能算稳定可用的生产级 Agent。最核心的问题不是页面或概念不够，而是：

> Agent 还没有把“真实工具执行、真实结果回传、状态一致收敛、可排障观测”这条链路彻底做实。

因此后续开发不应该优先追求复杂规划、多 runtime、子代理或平台化，而应该先把 Agent 做成一个可验证、可恢复、可排障的真实执行系统。

## 2. 总体推进原则

开发顺序建议遵循：

1. 先统一最小契约
2. 再打通单工具真实闭环
3. 再补齐异常和状态一致性
4. 再补可观测和诊断能力
5. 再升级意图识别和工具路由
6. 最后再做复杂规划、多 runtime 和平台化

一句话概括：

> 先把 Agent 做“真”，再把 Agent 做“稳”，再把 Agent 做“聪明”，最后把 Agent 做“强”。

## 3. 调整后的阶段规划

### 第 0 阶段：明确最小 Agent 契约

#### 目标

在继续开发真实执行链路之前，先明确 Agent 运行过程中几个核心对象的最小契约，避免后续反复返工。

这一阶段不追求完整平台化，也不要求大规模重构，只需要让团队对以下问题有统一答案：

- 一个 Agent run 的标准输入是什么
- 一个 Agent run 的终态有哪些
- tool call 由谁创建、谁执行、谁结算
- tool result 的标准结构是什么
- event 用于前端展示、调试追踪还是状态推进
- credits 冻结、释放、结算分别发生在哪些节点
- 同步工具和异步工具是否使用同一套状态语义

#### 关键改动

- 定义 `run` 最小状态机：pending、running、waiting_confirmation、waiting_tool、succeeded、failed、cancelled、timeout。
- 定义 `tool_call` 最小状态机：created、confirmation_required、approved、rejected、running、succeeded、failed、timeout、cancelled。
- 定义统一的 `tool_result` 结构，至少包含 success、data、summary、error_code、error_message、raw_output、usage、started_at、finished_at。
- 定义事件分层：用户可见事件、调试事件、系统状态事件。
- 明确 backend、agent-service、worker 三方职责边界。

#### 退出标准

- 能用一张状态流转图说明 run 和 tool call 的生命周期。
- 能明确回答“某个状态由哪个服务负责推进”。
- 能明确回答“工具成功、失败、超时、取消时分别写哪些表、发哪些事件、扣不扣 credits”。
- 后续第 1 阶段开发不再需要临时设计状态语义。

#### 风险

- 如果过度设计，会把第 0 阶段做成平台化重构。
- 如果契约定义太粗，后续真实工具执行时仍然会出现职责不清。

#### 建议控制方式

只定义当前真实工具闭环必须用到的最小契约，不提前设计复杂规划、多工具编排、子代理和 run replay 的完整模型。

---

### 第 1 阶段：打通单工具真实执行闭环

#### 目标

选一个最典型、最容易验证的工具，完成从用户请求到真实工具执行、结果回传、Agent 总结回复的完整闭环。

推荐优先选择：

- 小红书文案工具
- 或商品标题优化工具

这一阶段的重点不是覆盖所有工具，而是先证明 Agent 真的可以调用一个真实工具并基于真实结果继续工作。

#### 关键改动

- Agent 根据用户输入选择目标工具。
- 如果工具需要确认，正确创建确认事件并等待用户确认。
- 确认后由明确的一方触发真实工具执行。
- worker 执行真实工具逻辑，而不是返回占位 summary。
- 工具执行结果回写到统一 `tool_result`。
- Agent 能读取真实结果，并生成最终回复。
- 前端 timeline、message、tool call 状态保持一致。
- credits 在成功、失败、取消路径下能正确处理。

#### 退出标准

- 用户输入一个真实请求后，Agent 能自动选中目标工具。
- 用户确认后，真实 worker 任务被触发。
- 工具结果不是 mock，也不是占位文本。
- Agent 最终回复中使用了真实工具输出。
- 前端能看到完整过程：理解请求、选择工具、等待确认、执行工具、返回结果。
- 成功路径至少连续跑通 10 次无脏状态。

#### 验证用例

- 正常输入：用户明确要求生成小红书文案。
- 模糊输入：用户说“帮我写一篇适合发小红书的内容”。
- 取消确认：用户拒绝工具调用。
- 工具失败：worker 返回明确错误。
- 工具超时：worker 长时间未返回。

#### 风险

- 如果一开始试图兼容所有工具，范围会失控。
- 异步执行结果回传可能暴露 backend、agent-service、worker 的职责边界问题。
- credits、run 状态、tool call 状态可能出现不一致。

---

### 第 2 阶段：补齐异常处理和状态一致性

#### 目标

让 Agent 从“正常路径能跑通”升级为“异常路径也能稳定收尾”。

这一阶段重点解决脏状态、重复执行、重复扣费、长时间 pending、失败不可解释等问题。

#### 关键改动

- 明确 run、tool call、event 的幂等规则。
- 增加重复提交保护。
- 增加重复确认保护。
- 增加重复执行保护。
- 增加工具超时后的自动收敛。
- 增加取消后的状态收敛。
- 增加服务重启后的未完成任务扫描或补偿策略。
- 统一用户可见错误提示和内部错误码。

#### 退出标准

- 同一个 run 不会被重复执行。
- 同一个 tool call 不会被重复结算。
- 确认接口重复调用不会导致重复触发 worker。
- 工具失败后 run 一定进入可解释终态。
- 工具超时后 run 不会永久卡住。
- credits 在成功、失败、取消、超时路径下都符合预期。

#### 验证用例

- 连续点击确认按钮。
- 前端重复提交同一请求。
- agent-service 调 backend 失败。
- backend 调 worker 失败。
- worker 执行中服务重启。
- worker 成功但结果回写失败。
- 用户中途取消 run。

#### 风险

- 状态机补齐会牵涉多处已有逻辑。
- 一些之前没暴露的数据一致性问题会集中出现。
- 如果没有第 0 阶段的契约，这一阶段很容易变成局部补丁。

---

### 第 3 阶段：建立最小可观测和诊断能力

#### 目标

让 Agent 出问题后可以快速定位原因，而不是靠猜日志、读数据库和手动复现。

这一阶段不需要一开始就做完整 dashboard，但必须建立跨服务追踪和关键决策记录。

#### 关键改动

- 建立统一 `trace_id`，贯通 frontend、backend、agent-service、worker。
- 关键日志改成结构化日志。
- 记录关键决策：为什么判断这个 intent、为什么选择这个工具、为什么要求确认。
- 记录关键耗时：模型耗时、工具耗时、等待确认耗时、总 run 耗时。
- 记录失败原因：模型失败、工具失败、确认超时、预算不足、系统异常。
- 增加基础指标：run 成功率、失败率、平均耗时、工具成功率、工具失败率。

#### 退出标准

- 任意一条失败 run，能在 5 到 10 分钟内定位主要失败原因。
- 任意一条成功 run，能看到完整执行路径。
- 能通过同一个 `trace_id` 串起 backend、agent-service、worker 的日志。
- 能区分是模型问题、工具问题、用户取消、系统异常还是预算问题。

#### 风险

- 如果日志字段不统一，后续会越埋越乱。
- 如果事件和日志职责不清，前端展示事件可能被调试信息污染。
- 如果记录过多 raw 内容，可能带来存储和隐私风险。

---

### 第 4 阶段：升级意图识别和工具路由能力

#### 目标

让 Agent 从“规则驱动的工具路由器”升级为“规则 + 模型判断”的混合路由系统。

这一阶段应该在真实工具闭环稳定之后再做，否则无法有效评估路由质量。

#### 关键改动

- 保留确定性规则处理高置信请求。
- 对模糊请求引入模型判断。
- 对多工具候选引入打分和排序。
- 输出工具选择理由，用于调试和优化。
- 增加澄清问题生成能力。
- 收缩过强的关键词和长度硬判断。

#### 退出标准

- 明确请求能稳定命中正确工具。
- 模糊请求能生成合理澄清问题，而不是直接失败。
- 多工具候选时能给出可解释的排序。
- 常见闲聊不会被误判成工具调用。
- 路由升级后没有破坏第 1 阶段打通的真实工具链路。

#### 验证用例

- 闲聊类：“你是谁？”
- 短问句：“帮我处理一下。”
- 明确工具请求：“帮我写一篇小红书种草文。”
- 多工具歧义请求：“帮我优化这个商品内容。”
- 不支持请求：“帮我生成一段视频。”

#### 风险

- 引入模型判断后会增加成本和延迟。
- 如果规则和模型的优先级不清晰，会导致结果不稳定。
- 如果没有样本集，只靠人工感觉很难判断路由是否真的变好。

---

### 第 5 阶段：升级输出体验和交互质量

#### 目标

让 Agent 在用户体验上更自然、更实时、更可信。

这一阶段应该在事件语义和状态流转相对稳定之后再做，否则前端很容易被后端状态不一致拖累。

#### 关键改动

- 支持真正的流式输出，而不是服务端切片伪流式。
- 优化工具确认交互。
- 优化等待态：正在理解、正在选择工具、等待确认、正在执行工具、正在生成回复。
- 优化取消、失败、超时后的用户提示。
- 让 timeline 更清楚地表达 Agent 当前在做什么。

#### 退出标准

- 长回复能实时输出。
- 工具执行期间用户知道 Agent 正在等待什么。
- 失败时用户能知道是工具失败、模型失败、预算不足还是系统异常。
- 弱网或断流后前端能恢复到可理解状态。
- 非开发用户能理解 timeline 的主要含义。

#### 风险

- 真流式会增加前后端协议复杂度。
- 前端状态管理不稳时，流式输出容易引入竞态。
- 如果后端事件语义不稳定，前端体验优化会反复返工。

---

### 第 6 阶段：明确 runtime 战略和复杂规划能力

#### 目标

让系统能够根据任务复杂度选择不同执行策略，并逐步支持多步规划、多工具协作和失败回退。

这一阶段不应该过早开始。只有当前面的真实执行、状态一致性、可观测性都稳定后，多 runtime 和复杂规划才有意义。

#### 关键改动

- 明确 LangGraph 路线和 Deep Agents 路线的主次关系。
- 明确哪些请求走轻量 runtime，哪些请求走复杂 runtime。
- 将 runtime 选择从全局开关升级为 per-run 策略。
- 支持多步计划和阶段性结果。
- 支持多工具串联和中间结果传递。
- 为复杂任务增加失败回退和阶段性收敛。

#### 退出标准

- 简单请求仍然走低成本、低延迟路径。
- 复杂请求能进入多步规划路径。
- runtime 选择有明确理由和日志记录。
- 多步任务中的中间状态、工具结果、预算消耗可解释。
- 新增复杂 runtime 不破坏已有简单工具调用。

#### 风险

- 多 runtime 并存会显著增加维护成本。
- 如果前面契约不稳，这一阶段会放大所有状态问题。
- 复杂规划能力容易变成“看起来强，但实际不可控”。

---

### 第 7 阶段：沉淀平台化能力

#### 目标

把 Agent 从单一功能模块沉淀为可以持续扩展、持续治理、持续评估的平台能力。

这一阶段关注长期演进，不应该在基础链路还没稳定前提前展开。

#### 关键改动

- 完善 Agent 契约层，而不是只保留最小契约。
- 建立 prompt、tool、runtime、policy、event 的版本管理。
- 引入 run replay。
- 引入效果评估样本集和自动评测。
- 支持更多业务工具低成本接入。
- 支持 Agent 行为分析和质量趋势观察。

#### 退出标准

- 新增一个工具时，接入成本明显下降。
- 同一套 Agent 基础设施能复用到多个业务场景。
- 出问题时可以通过 replay、日志、指标快速定位。
- 团队成员能清楚判断某类问题应该改哪一层。
- 有固定评估集衡量 Agent 质量变化，而不是只靠主观体验。

#### 风险

- 平台化会增加前期投入。
- 如果基础链路没稳，平台化会放大复杂度。
- 如果没有真实业务场景驱动，平台化容易过度抽象。

## 4. 推荐近期最小开发路线

如果目标是尽快把当前 Agent 从 Alpha 推到可用状态，建议近期只聚焦前四个阶段：

1. 第 0 阶段：最小契约确认
2. 第 1 阶段：单工具真实执行闭环
3. 第 2 阶段：异常处理和状态一致性
4. 第 3 阶段：最小可观测和诊断能力

不要在这四件事完成之前投入大量时间做：

- 多 runtime
- deep agents
- 子代理
- 复杂任务规划
- 完整平台化
- 大规模 UI 动效优化

这些能力不是不重要，而是它们依赖前面的真实执行链路和状态基础。

## 5. 第一轮建议交付物

第一轮开发可以定义为一个非常具体的目标：

> 小红书文案工具在 Agent 中真实执行闭环可用。

### 交付范围

- 用户在 Agent 页面输入小红书文案需求。
- Agent 能识别并选择小红书文案工具。
- 如果需要确认，前端展示确认卡片。
- 用户确认后，后端触发真实 worker 工具执行。
- worker 返回真实结果。
- Agent 基于真实结果生成最终回复。
- 前端 timeline、message、tool call 状态一致。
- credits 在成功、失败、取消、超时路径下正确处理。

### 不在第一轮范围内

- 支持所有工具。
- 支持多工具串联。
- 支持复杂规划。
- 支持 deep agents 默认启用。
- 支持完整 run replay。
- 支持完整指标 dashboard。
- 支持 token-level 真流式。

### 第一轮验收清单

- 成功路径连续跑通 10 次。
- 工具失败时 run 正确失败并展示可理解错误。
- 工具超时时 run 正确收敛。
- 用户拒绝确认时不会触发工具执行。
- 重复点击确认不会重复执行工具。
- credits 不会重复扣除。
- 日志中能通过同一个 trace id 找到完整链路。

## 6. 最大风险和控制建议

### 风险一：第一阶段范围过大

控制方式：只打通一个工具，不要一开始兼容所有工具。

### 风险二：契约后置导致返工

控制方式：先做最小契约，不做完整平台化。

### 风险三：状态一致性被低估

控制方式：把失败、超时、取消、重复提交作为必测路径，而不是上线前补丁。

### 风险四：过早做复杂 Agent

控制方式：多 runtime、Deep Agents、子代理必须等真实执行和可观测稳定后再进入主路径。

### 风险五：没有评估样本

控制方式：从第 1 阶段开始沉淀最小样本集，包括正常请求、模糊请求、失败请求和边界请求。

## 7. 最终结论

原始开发计划的大方向是正确的，但需要做两个关键调整：

1. 把“统一 Agent 契约层”从后期平台化中前置为最小契约。
2. 把“真实工具执行闭环”拆小，先用一个典型工具跑通垂直链路。

调整后的计划更适合实际落地：

> 先定最小契约，再打穿单工具闭环，再补异常和观测，然后再做智能路由、体验升级和复杂 Agent 能力。

这样推进可以避免系统在基础不稳时过早进入高复杂度状态，也能让每一阶段都有清晰的交付物、退出标准和验证方法。
