# 2号成员用户端接口调用说明

适用对象：2号用户端前端开发。

当前用户端要先完成 V1 主链路：

```text
注册/登录 -> 查看工具分类 -> 查看工具列表 -> 查看工具详情 -> 查看算力 -> 创建任务 -> 查询任务状态 -> 查询任务详情/结果 -> 我的任务列表
```

接口基准文件：

```text
backend/src/main/resources/openapi.yml
```

本地后端地址：

```text
http://localhost:8080
```

## 1. 通用响应格式

所有接口统一返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {},
  "requestId": null
}
```

前端判断是否成功时，优先判断：

```text
code === "SUCCESS"
```

常见错误：

```text
PARAM_ERROR          参数错误
UNAUTHORIZED         未登录或 Token 失效
TOOL_NOT_FOUND       工具不存在或未上线
CREDIT_NOT_ENOUGH    算力不足
TASK_NOT_FOUND       任务不存在
SYSTEM_ERROR         系统异常
```

## 2. 登录态处理

注册和登录成功后，后端返回：

```json
{
  "accessToken": "jwt-token",
  "user": {
    "id": 2,
    "username": "user1",
    "nickname": "User One",
    "userType": "USER"
  }
}
```

前端需要保存：

```text
data.accessToken
```

后续需要登录的接口统一加请求头：

```http
Authorization: Bearer <accessToken>
```

## 3. 用户注册

```http
POST /api/v1/auth/register
```

请求体：

```json
{
  "username": "test001",
  "password": "123456",
  "nickname": "测试用户"
}
```

字段说明：

```text
username 必填，用户名，不能重复
password 必填，6-64 位
nickname 可选，不传时默认使用 username
```

注册成功后会直接返回 `accessToken`，前端可以直接进入登录态。

## 4. 用户登录

```http
POST /api/v1/auth/login
```

请求体：

```json
{
  "account": "user1",
  "password": "123456"
}
```

注意：登录字段叫 `account`，注册字段叫 `username`。

## 5. 获取当前用户

```http
GET /api/v1/users/me
Authorization: Bearer <accessToken>
```

返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "id": 2,
    "username": "user1",
    "nickname": "User One",
    "userType": "USER"
  },
  "requestId": null
}
```

## 6. 获取工具分类

```http
GET /api/v1/tool-categories
```

当前该接口无需登录。

返回重点：

```json
{
  "data": [
    {
      "id": 1,
      "categoryCode": "copywriting",
      "categoryName": "文案生成",
      "sortOrder": 1
    }
  ]
}
```

## 7. 获取工具列表

```http
GET /api/v1/tools
```

当前该接口无需登录，只返回已上线工具。

返回结构：

```json
{
  "data": {
    "list": [
      {
        "id": 1,
        "toolCode": "xiaohongshu_copywriting",
        "toolName": "小红书文案生成",
        "categoryId": 1,
        "categoryName": "文案生成",
        "description": "根据产品信息生成小红书文案",
        "coverUrl": "",
        "status": "ONLINE",
        "estimatedCreditCost": 10
      }
    ],
    "total": 1
  }
}
```

当前暂不支持分页参数、分类筛选、关键词搜索。前端先按全量列表渲染。

## 8. 获取工具详情

```http
GET /api/v1/tools/{toolCode}
```

示例：

```http
GET /api/v1/tools/xiaohongshu_copywriting
```

返回重点：

```json
{
  "data": {
    "id": 1,
    "toolCode": "xiaohongshu_copywriting",
    "toolName": "小红书文案生成",
    "status": "ONLINE",
    "estimatedCreditCost": 10,
    "fields": [
      {
        "fieldKey": "productName",
        "fieldName": "产品名称",
        "fieldType": "text",
        "placeholder": "请输入产品名称",
        "options": null,
        "required": true,
        "sortOrder": 1
      },
      {
        "fieldKey": "targetCustomer",
        "fieldName": "目标用户",
        "fieldType": "textarea",
        "placeholder": "请输入目标用户",
        "options": null,
        "required": true,
        "sortOrder": 2
      },
      {
        "fieldKey": "style",
        "fieldName": "文案风格",
        "fieldType": "select",
        "placeholder": "请选择文案风格",
        "options": [
          { "label": "种草", "value": "种草" },
          { "label": "专业", "value": "专业" }
        ],
        "required": true,
        "sortOrder": 3
      }
    ]
  }
}
```

动态表单渲染规则：

```text
fieldType = text       单行输入框
fieldType = textarea   多行输入框
fieldType = select     下拉选择，读取 options
required = true        前端需要做必填校验
```

提交任务时，表单值放到 `params`，key 使用 `fieldKey`。

## 9. 获取算力账户

```http
GET /api/v1/credits/account
Authorization: Bearer <accessToken>
```

新用户首次访问会默认创建 100 点算力。

返回：

```json
{
  "data": {
    "accountId": 1,
    "userId": 2,
    "balance": 100,
    "frozen": 0,
    "totalGranted": 100,
    "totalConsumed": 0,
    "status": "ACTIVE"
  }
}
```

页面展示建议：

```text
当前算力：balance
已消耗：totalConsumed
```

## 10. 创建任务

```http
POST /api/v1/tasks
Authorization: Bearer <accessToken>
```

请求体：

```json
{
  "toolCode": "xiaohongshu_copywriting",
  "params": {
    "productName": "真实测试产品",
    "targetCustomer": "年轻用户",
    "style": "种草"
  },
  "clientRequestId": "uuid-from-frontend"
}
```

字段说明：

```text
toolCode         工具编码，来自工具详情
params           动态表单值，key 必须用 fields[].fieldKey
clientRequestId  前端生成的唯一请求 ID，建议用 uuid
```

返回：

```json
{
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "status": "QUEUED",
    "progress": 0,
    "progressMessage": "任务已排队"
  }
}
```

创建成功后会扣减工具配置的算力：

```text
扣减数量 = 工具 estimatedCreditCost
```

如果算力不足，返回：

```json
{
  "code": "CREDIT_NOT_ENOUGH",
  "message": "算力不足",
  "data": null
}
```

## 11. 查询任务状态

```http
GET /api/v1/tasks/{taskId}/status
Authorization: Bearer <accessToken>
```

返回：

```json
{
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "status": "PROCESSING",
    "progress": 45,
    "progressMessage": "AI 正在生成内容"
  }
}
```

状态值：

```text
QUEUED      已排队
PROCESSING 处理中
SUCCESS    成功
FAILED     失败
```

当前 V1 先用轮询：

```text
每 2-3 秒请求一次 /api/v1/tasks/{taskId}/status
当 status 为 SUCCESS 或 FAILED 时停止轮询，再请求任务详情
```

## 12. 查询任务详情

```http
GET /api/v1/tasks/{taskId}
Authorization: Bearer <accessToken>
```

成功任务返回：

```json
{
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "toolCode": "xiaohongshu_copywriting",
    "toolName": "小红书文案生成",
    "status": "SUCCESS",
    "progress": 100,
    "progressMessage": "生成完成",
    "params": {
      "productName": "真实测试产品",
      "targetCustomer": "年轻用户",
      "style": "种草"
    },
    "result": {
      "resourceType": "MARKDOWN",
      "contentText": "# 生成结果"
    },
    "createdAt": "2026-05-08T15:35:00",
    "finishedAt": "2026-05-08T15:35:10"
  }
}
```

结果展示：

```text
result.resourceType = MARKDOWN 时，直接按 Markdown/纯文本展示 contentText
result 为 null 时，说明任务还没有结果
```

## 13. 我的任务列表

```http
GET /api/v1/tasks
Authorization: Bearer <accessToken>
```

返回：

```json
{
  "data": {
    "list": [
      {
        "taskId": 1,
        "taskNo": "T20260508AF040D708A",
        "toolCode": "xiaohongshu_copywriting",
        "toolName": "小红书文案生成",
        "status": "SUCCESS",
        "progress": 100,
        "result": {
          "resourceType": "MARKDOWN",
          "contentText": "# 生成结果"
        }
      }
    ],
    "total": 1
  }
}
```

当前暂不支持分页参数，先按全量列表渲染。

## 14. 用户端推荐联调顺序

1. 登录 `POST /api/v1/auth/login`
2. 获取当前用户 `GET /api/v1/users/me`
3. 获取算力 `GET /api/v1/credits/account`
4. 获取分类 `GET /api/v1/tool-categories`
5. 获取工具列表 `GET /api/v1/tools`
6. 进入工具详情 `GET /api/v1/tools/{toolCode}`
7. 根据 `fields` 渲染动态表单
8. 创建任务 `POST /api/v1/tasks`
9. 轮询任务状态 `GET /api/v1/tasks/{taskId}/status`
10. 查看任务详情 `GET /api/v1/tasks/{taskId}`
11. 回到我的任务列表 `GET /api/v1/tasks`

## 15. 注意事项

- `application/json` 请求体里如果有中文，建议明确使用 UTF-8。
- 登录返回字段是 `accessToken`，不是 `token`。
- 工具 ID 字段当前叫 `id`，任务 ID 字段叫 `taskId`。
- 用户端只能看已上线工具。
- 任务创建后真实 AI 结果由 3 号 Worker 回写，前端不要假设创建后立刻成功。
- 当前没有 SSE，先用轮询。

```

```

