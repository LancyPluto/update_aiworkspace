# 3号成员 Worker 接口调用说明

适用对象：3号 AI Worker 开发。

Worker 负责从后端获取任务上下文，调用模型生成内容，然后把任务状态和结果回写给后端。

当前 Worker 最小链路：

```text
获取任务上下文 -> 标记处理中 -> 调用 AI -> 成功回写结果
                                     -> 失败回写错误
```

接口基准文件：

```text
backend/src/main/resources/openapi.yml
```

本地后端地址：

```text
http://localhost:8080
```

## 1. 当前内部接口鉴权说明

当前 V1 本地联调阶段，内部接口暂时放行：

```text
/api/internal/v1/**
```

也就是说，当前 Worker 调用内部接口时暂时不需要 `Authorization`。

后续联调稳定后会补最小内部安全机制：

```http
X-Internal-Token: <internal-token>
```

所以 3 号开发时建议把内部 token 预留成配置项，不要写死。

## 2. 获取任务执行上下文

```http
GET /api/internal/v1/tasks/{taskId}/execution-context
```

示例：

```http
GET /api/internal/v1/tasks/1/execution-context
```

返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "userId": 2,
    "toolId": 1,
    "toolCode": "xiaohongshu_copywriting",
    "toolName": "小红书文案生成",
    "status": "QUEUED",
    "params": {
      "productName": "真实测试产品",
      "targetCustomer": "年轻用户",
      "style": "种草"
    },
    "fields": [
      {
        "fieldKey": "productName",
        "fieldName": "产品名称",
        "fieldType": "text",
        "required": true,
        "sortOrder": 1
      }
    ]
  },
  "requestId": null
}
```

Worker 使用重点：

```text
taskId      回写状态和结果时使用
toolCode    判断调用哪类 AI 生成逻辑
params      用户提交的表单参数
fields      工具字段配置，可用于校验或组装 Prompt
```

当前后端暂未提供 Prompt 模板接口，所以 Worker 可以先按 `toolCode` 写本地生成逻辑。

## 3. 标记任务处理中

```http
POST /api/internal/v1/tasks/{taskId}/processing
Content-Type: application/json
```

请求：

```json
{
  "progress": 35,
  "progressMessage": "AI 正在生成内容"
}
```

返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "status": "PROCESSING",
    "progress": 35,
    "progressMessage": "AI 正在生成内容"
  },
  "requestId": null
}
```

规则：

```text
progress 建议 1-99
如果不传 progress，后端默认 10
如果 progress 超过 99，后端会限制为 99
```

建议 Worker 在正式调用模型前先调这个接口，用户端就能看到任务正在处理。

## 4. 成功回写结果

```http
POST /api/internal/v1/tasks/{taskId}/success
Content-Type: application/json
```

请求：

```json
{
  "resourceType": "MARKDOWN",
  "contentText": "# 小红书文案生成结果\n\n这里是生成内容。"
}
```

字段说明：

```text
resourceType 当前固定用 MARKDOWN
contentText  模型生成的最终文本
```

返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "status": "SUCCESS",
    "progress": 100,
    "progressMessage": "生成完成"
  },
  "requestId": null
}
```

成功回写后，用户端调用：

```http
GET /api/v1/tasks/{taskId}
```

会看到：

```json
{
  "result": {
    "resourceType": "MARKDOWN",
    "contentText": "# 小红书文案生成结果\n\n这里是生成内容。"
  }
}
```

## 5. 失败回写

```http
POST /api/internal/v1/tasks/{taskId}/failed
Content-Type: application/json
```

请求：

```json
{
  "errorCode": "MODEL_CALL_FAILED",
  "errorMessage": "模型调用超时"
}
```

返回：

```json
{
  "code": "SUCCESS",
  "message": "ok",
  "data": {
    "taskId": 1,
    "taskNo": "T20260508AF040D708A",
    "status": "FAILED",
    "progress": 100,
    "progressMessage": "模型调用超时"
  },
  "requestId": null
}
```

推荐错误码：

```text
MODEL_CALL_FAILED    模型调用失败
SYSTEM_ERROR         Worker 内部异常
PARAM_ERROR          参数无法处理
```

## 6. Worker 本地联调流程

前置条件：

```text
1. 后端服务启动在 http://localhost:8080
2. 后台已经创建并发布工具
3. 用户端或接口工具已经创建任务，得到 taskId
```

联调步骤：

1. 获取任务上下文

```http
GET /api/internal/v1/tasks/{taskId}/execution-context
```

1. 标记处理中

```http
POST /api/internal/v1/tasks/{taskId}/processing
```

1. 调用 AI 模型生成文本
2. 成功时回写：

```http
POST /api/internal/v1/tasks/{taskId}/success
```

1. 失败时回写：

```http
POST /api/internal/v1/tasks/{taskId}/failed
```

1. 让 2 号用户端查询：

```http
GET /api/v1/tasks/{taskId}
```

确认 `result.contentText` 已展示。

## 7. 推荐 Worker 伪代码

```js
async function runTask(taskId) {
  const context = await get(`/api/internal/v1/tasks/${taskId}/execution-context`);

  await post(`/api/internal/v1/tasks/${taskId}/processing`, {
    progress: 20,
    progressMessage: "AI 正在生成内容"
  });

  try {
    const prompt = buildPrompt(context.data.toolCode, context.data.params, context.data.fields);
    const contentText = await callModel(prompt);

    await post(`/api/internal/v1/tasks/${taskId}/success`, {
      resourceType: "MARKDOWN",
      contentText
    });
  } catch (error) {
    await post(`/api/internal/v1/tasks/${taskId}/failed`, {
      errorCode: "MODEL_CALL_FAILED",
      errorMessage: error.message || "模型调用失败"
    });
  }
}
```

## 8. 注意事项

- 请求体有中文时，必须使用 UTF-8。
- 当前还没有“拉取待处理任务列表”接口，任务 ID 先由联调人员手动提供。
- 当前没有 Prompt 配置接口，Worker 先按 `toolCode` 写本地 Prompt 逻辑。
- 成功回写只支持一个主结果，保存到 `ai_result_resources`。
- 不要重复多次调用 success，否则当前会插入多条结果记录；V1 联调时保证 Worker 幂等即可。
- 内部接口当前未加密，只能用于本地开发和演示环境。

